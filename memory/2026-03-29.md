- User identity/preferences: Brendan; call them "human" in normal interaction.
- Standing priority rule: Aries AI is the default top priority unless human explicitly reprioritizes something else.
- Operating posture: act as executive assistant / lead agent for Aries AI; delegate by default for cleanly parallelizable tasks and for larger/longer tasks.
- Decision rule: make ambiguous decisions autonomously, then report the decision and rationale.
- Update policy: do not send routine updates; only update when there is a meaningful decision, blocker, or completed chunk of work.
- Required progress-report format: current state, what changed, blockers, decisions needed from me, next actions.
- Standing escalation list: always escalate before action on production deploys, client-facing messages, spending, credential/auth changes, deleting data, database schema changes, infra changes that can cause downtime, external publishing, legal/financial commitments, and anything irreversible or high-risk.
- Multi-agent direction: maintain a formal operating structure now; start lean and expand only when workload justifies it.
- Mission Control directive: build Mission Control as a separate isolated project outside `/app/aries-app`, under a separate projects path / agent directory; do not place Mission Control work inside `aries-app`.
- Mission Control product structure: three apps/modules — Ops, Brain, and Lab; Mac OSX-style floating bottom dock; draggable non-closable top tabs; per-module tool selector area; Mission Control must show current model, active sessions, and cron health.
- Accessibility/runtime directive for Mission Control: use fixed port 4174 and bind to 0.0.0.0; do not reuse port 3000.
- Strong standing rule from user: do not use mock data for anything being built; mock data should never be used, no exceptions.
- Heartbeat/state isolation directive: heartbeat/status output must stay out of the main restored chat, remain file-based unless explicitly requested in chat, and use an isolated state path outside `aries-app`.
