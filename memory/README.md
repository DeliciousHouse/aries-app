# memory/

This directory is operator-local and starts empty on a fresh clone.

Operator tooling may write dated Markdown notes here at runtime. These files are gitignored — they contain instance-specific session context and should never be committed.
