T28 — End-to-end weekly social-content smoke script (scripts/smoke-weekly-pipeline.mjs)

Status: SCRIPT IMPLEMENTED. The live integration green run is the script's
own self-check executed in a configured test environment with:
  - Local app running at APP_BASE_URL
  - DB_* env pointing at the test database
  - .env.test providing META_TEST_PAGE_ID, META_TEST_PAGE_ACCESS_TOKEN,
    OAUTH_TOKEN_ENCRYPTION_KEY, NEXTAUTH_SECRET
  - Hermes test environment delivering authenticated callbacks to
    ${APP_BASE_URL}/api/internal/hermes/runs
  - playwright installed (npm install --no-save playwright)

CLI surface verified locally:
  $ node scripts/smoke-weekly-pipeline.mjs --help                  -> exit 0
  $ node scripts/smoke-weekly-pipeline.mjs                         -> exit 1
  $ node scripts/smoke-weekly-pipeline.mjs --tenant t --website x --auto-approve
                                                                   -> exit 1 (invalid URL)
  $ node --check scripts/smoke-weekly-pipeline.mjs                 -> ok

Step roster enforced by the script (logged as numbered PASS lines on green run):
  PASS 1. signup new test tenant
  PASS 2. submit business profile via /api/onboarding
  PASS 3. connect Meta using test app token
  PASS 4. trigger /api/social-content/jobs
  PASS 5. poll runtime state until plan_review
  PASS 6. auto-approve plan
  PASS 7. wait for creative_review
  PASS 8. auto-approve creatives
  PASS 9. wait for publish_review
  PASS 10. auto-approve publish
  PASS 11. wait for completed
  PASS 12. assert posts.platform_post_id captured
  PASS 13. Graph API GET round-trip 200
  PASS 14. dashboard image rendering (naturalWidth > 0)

Focused unit coverage (tests/smoke-weekly-pipeline.test.ts):
  ok 1  - parseArgs accepts exact CLI form and rejects malformed input
  ok 2  - validateArgs flags every missing or malformed required flag
  ok 3  - validateEnv returns the names of every missing required env var
  ok 4  - encryptOAuthToken matches the production AES-256-GCM JSON envelope
  ok 5  - encryptOAuthToken rejects a missing or wrong-length key
  ok 6  - buildUsageText lists all 14 numbered steps in order
  ok 7  - STEP_LABELS exposes 14 numbered labels in order
  ok 8  - isAtApproval matches only when the runtime exposes the requested approval step
  ok 9  - isCompleted recognizes the social-content and marketing dialect state names
  ok 10 - CLI exits non-zero when required arguments are missing
  ok 11 - CLI --help prints the usage block and exits 0
  ok 12 - script source bans skip flags, soft-fail mode, and production Meta credentials
  ok 13 - SmokeError carries the failing step number through caller code
  ok 14 - EVIDENCE_PATHS points at the documented .sisyphus/evidence locations
  -> 14/14 PASS

Validation:
  node --check scripts/smoke-weekly-pipeline.mjs                   -> ok
  tsx --test tests/smoke-weekly-pipeline.test.ts                   -> 14/14 pass
  node scripts/check-banned-patterns.mjs                           -> ok
  node scripts/check-repo-boundary.mjs                             -> ok

Live final-qa artifacts (graph-verify.txt, dashboard-posts-<slug>.png/json) and
the green-run summary are written by the script during a real green run; they
are NOT pre-committed.
