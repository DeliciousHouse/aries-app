TASK 7: DB Schema Migration — Posts + Vision QA + Scheduled Posts + OAuth Callback Tokens
================================================================================

SCHEMA CHANGES IMPLEMENTED:
================================================================================

1. POSTS TABLE ADDITIONS
   - platform_post_id TEXT (nullable)
   - published_at TIMESTAMPTZ (nullable)
   - scheduled_at TIMESTAMPTZ (nullable)
   - published_status TEXT with CHECK constraint (draft, in_review, approved, scheduled, publishing, published, failed, rolled_back)
   - Index: idx_posts_tenant_created (tenant_id, created_at DESC)

2. NEW TABLE: vision_qa_runs
   - id BIGSERIAL PRIMARY KEY
   - tenant_id INTEGER NOT NULL (FK to organizations)
   - post_id BIGINT (nullable)
   - creative_id BIGINT (nullable)
   - attempt_number INTEGER DEFAULT 1
   - brand_color_match_score NUMERIC (nullable)
   - text_legibility_score NUMERIC (nullable)
   - forbidden_pattern_hits INTEGER (nullable)
   - brand_violation_score NUMERIC (nullable)
   - verdict TEXT with CHECK constraint (pass, fail, operator_override)
   - model_version TEXT (nullable)
   - raw_model_output JSONB (nullable)
   - created_at TIMESTAMPTZ DEFAULT now()
   - Index: idx_vision_qa_runs_tenant_post (tenant_id, post_id)

3. NEW TABLE: scheduled_posts
   - id BIGSERIAL PRIMARY KEY
   - post_id BIGINT UNIQUE
   - tenant_id INTEGER NOT NULL (FK to organizations)
   - scheduled_for TIMESTAMPTZ NOT NULL
   - target_platforms TEXT[] DEFAULT '{}'
   - updated_at TIMESTAMPTZ DEFAULT now()
   - Index: idx_scheduled_posts_tenant_scheduled (tenant_id, scheduled_for)

4. NEW TABLE: oauth_callback_tokens
   - token_hash CHAR(64) PRIMARY KEY
   - aries_run_id TEXT NOT NULL
   - tenant_id INTEGER NOT NULL (FK to organizations)
   - issued_at TIMESTAMPTZ DEFAULT now()
   - consumed_at TIMESTAMPTZ (nullable)
   - Index: idx_oauth_callback_tokens_aries_run_id (aries_run_id)

5. CREATIVE_ASSETS TABLE ADDITION
   - superseded_by BIGINT (nullable) — for regeneration tracking

IDEMPOTENCY VERIFICATION:
================================================================================

All SQL uses idempotent patterns:
✓ CREATE TABLE IF NOT EXISTS
✓ ALTER TABLE ... ADD COLUMN IF NOT EXISTS
✓ CREATE INDEX IF NOT EXISTS
✓ CHECK constraints defined inline (no separate ALTER)

This allows safe re-running on existing databases without errors.

TYPESCRIPT TYPES CREATED:
================================================================================

✓ types/posts.ts
  - PublishedStatus type (union of valid statuses)
  - Post interface (full row type)
  - CreatePostInput interface
  - UpdatePostInput interface

✓ types/vision-qa.ts
  - VisionQAVerdict type (union of valid verdicts)
  - VisionQARun interface (full row type)
  - CreateVisionQARunInput interface
  - UpdateVisionQARunInput interface

✓ types/scheduled-posts.ts
  - ScheduledPost interface (full row type)
  - CreateScheduledPostInput interface
  - UpdateScheduledPostInput interface

VALIDATION RESULTS:
================================================================================

✓ npm run typecheck: PASSED (0 errors)
  - All new type files compile without errors
  - No `as any`, `@ts-ignore`, or type violations
  - Strict mode enabled (TS 5.7.3)

✓ JavaScript syntax check: PASSED
  - scripts/init-db.js syntax valid

✓ SQL syntax: VALID
  - All CREATE TABLE statements use standard PostgreSQL syntax
  - All ALTER TABLE statements use IF NOT EXISTS pattern
  - All CHECK constraints properly formatted
  - All indexes properly named with IF NOT EXISTS

IMPLEMENTATION NOTES:
================================================================================

1. Foreign Keys:
   - All new tables reference organizations(id) with ON DELETE CASCADE
   - No circular dependencies
   - Soft references used (no FK constraints on post_id/creative_id in vision_qa_runs)

2. Naming Conventions:
   - Table names: snake_case (posts, vision_qa_runs, scheduled_posts, oauth_callback_tokens)
   - Column names: snake_case
   - Index names: idx_{table}_{columns} pattern
   - Type names: PascalCase (Post, VisionQARun, ScheduledPost)

3. Constraints:
   - CHECK constraints for enum-like columns (published_status, verdict)
   - UNIQUE constraint on scheduled_posts.post_id
   - PRIMARY KEY constraints on all tables
   - NOT NULL constraints where required

4. Timestamps:
   - All use TIMESTAMPTZ for timezone awareness
   - DEFAULT now() for created_at/issued_at
   - Nullable for optional timestamps (published_at, scheduled_at, consumed_at)

5. Array Types:
   - target_platforms uses TEXT[] with DEFAULT '{}'
   - Matches existing pattern in oauth_connections table

NEXT STEPS:
================================================================================

1. Commit changes:
   git add scripts/init-db.js types/posts.ts types/vision-qa.ts types/scheduled-posts.ts
   git commit -m "feat(db): posts.platform_post_id/published_at/scheduled_at + vision_qa_runs + scheduled_posts + oauth_callback_tokens"

2. Update plan checkbox:
   .sisyphus/plans/weekly-social-content-pipeline.md line 674
   Change: - [ ] **T7. to - [x] **T7.

3. On fresh database:
   npm run db:init
   (Will create all tables and columns)

4. On existing database:
   npm run db:init
   (Will add missing columns and tables, skip existing ones)

EVIDENCE GENERATED:
================================================================================
Date: 2026-05-06
Task: T7 — DB Schema Migration
Status: COMPLETE
Verification: All checks passed
