{
  "artifact_name": "marketing_job_contract",
  "schema_version": "1.0.0",
  "status": "ready",
  "canonical_inputs": {
    "tenant_runtime_state_schema": "./specs/tenant_runtime_state_schema.v1.json",
    "marketing_job_state_schema": "./specs/marketing_job_state_schema.v1.json"
  },
  "contract": {
    "start_endpoint": {
      "function": "startMarketingJob",
      "request": {
        "type": "object",
        "required": [
          "tenantId",
          "jobType"
        ],
        "properties": {
          "tenantId": {
            "description": "Maps to tenant_runtime_state_schema.$defs.tenant_id",
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]{1,61}$"
          },
          "jobType": {
            "description": "Canonical marketing job type",
            "type": "string",
            "enum": [
              "brand_campaign"
            ]
          },
          "payload": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "additionalProperties": false
      },
      "response": {
        "type": "object",
        "required": [
          "status",
          "jobId",
          "tenantId",
          "jobType"
        ],
        "properties": {
          "status": {
            "const": "accepted"
          },
          "jobId": {
            "description": "Must satisfy marketing_job_state_schema.properties.job_id",
            "type": "string",
            "pattern": "^mkt_[a-zA-Z0-9._:-]{3,127}$"
          },
          "tenantId": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]{1,61}$"
          },
          "jobType": {
            "type": "string",
            "enum": [
              "brand_campaign"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    "status_endpoint": {
      "function": "getMarketingJobStatus",
      "request": {
        "type": "object",
        "required": [
          "jobId"
        ],
        "properties": {
          "jobId": {
            "description": "Must satisfy marketing_job_state_schema.properties.job_id",
            "type": "string",
            "pattern": "^mkt_[a-zA-Z0-9._:-]{3,127}$"
          }
        },
        "additionalProperties": false
      },
      "response": {
        "type": "object",
        "required": [
          "jobId",
          "status"
        ],
        "properties": {
          "jobId": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9][a-zA-Z0-9._:-]{1,127}$"
          },
          "status": {
            "description": "Uses job runtime state vocabulary",
            "type": "string",
            "enum": [
              "queued",
              "running",
              "completed",
              "failed"
            ]
          }
        },
        "additionalProperties": false
      }
    }
  },
  "hard_failure_policy": {
    "when": [
      "./specs/tenant_runtime_state_schema.v1.json missing",
      "./specs/marketing_job_state_schema.v1.json missing",
      "./specs/tenant_runtime_state_schema.v1.json invalid JSON",
      "./specs/marketing_job_state_schema.v1.json invalid JSON"
    ],
    "behavior": "throw Error with prefix HARD_FAILURE"
  }
}
