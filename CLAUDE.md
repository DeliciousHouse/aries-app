# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Aries AI is a Next.js 16 App Router application for marketing automation. It combines a public marketing site, an authenticated operator shell, and browser-safe internal APIs that submit workflow execution to Hermes while keeping runtime state on the server.

## Architecture

```
Browser
  -> Next.js pages (app/*)
  -> Next.js route handlers (app/api/*)
      -> Aries backend services (backend/*, lib/*)
          -> Hermes Gateway for workflow submission
Hermes reconciler
  -> Hermes Gateway for run polling
  -> idempotent Aries ingestion
Aries backend services
  -> PostgreSQL + runtime files under DATA_ROOT for state and read models
```

**Key architectural rules:**
- Aries owns the browser boundary. The UI talks only to Next.js route handlers.
- Long-running workflow execution is submitted to Hermes and durably polled by the Aries reconciler — never exposed directly to the browser.
- Route handlers return frontend-safe payloads; never leak raw runtime files or internal workflow details.
- All marketing, integrations, and approval flows are tenant-aware and validated server-side.

**Path alias:** `@/*` maps to the repo root (configured in tsconfig.json).

**Runtime paths:** `lib/runtime-paths.ts` resolves `CODE_ROOT` (repo checkout) and `DATA_ROOT` (generated runtime artifacts) with fallback logic for container vs local environments. Generated data lives under `DATA_ROOT/generated/` with `draft/` and `validated/` subdirectories.

### Execution Provider Pattern

Hermes is the sole execution provider (`ARIES_EXECUTION_PROVIDER=hermes`, `ARIES_MARKETING_EXECUTION_PROVIDER=hermes`; these env vars are retained as forward-compatible selectors). Aries creates an execution run record before submitting to Hermes. The standing reconciler polls Hermes and passes terminal results through the idempotent ingestion handler; `/api/internal/hermes/runs` remains the secret-authenticated ingestion seam, but Hermes does not invoke the submitted `callback_url`. The execution-provider seam (`backend/execution/`, `backend/marketing/execution-port.ts`) is kept as a single-provider abstraction. Aries submits generic media-render requests and Hermes owns provider selection and execution.

### Marketing Pipeline (4-Stage Workflow)

The core domain flow is a 4-stage marketing pipeline executed by Hermes:
1. **Research** (`stage-1-research`) — competitor analysis, ad library scraping
2. **Strategy** (`stage-2-strategy`) — social content strategy from research
3. **Production** (`stage-3-production`) — creative/content generation
4. **Publish & Optimize** (`stage-4-publish-optimize`) — publishing and performance tracking

Each stage can pause for human approval. `backend/marketing/orchestrator.ts` drives the pipeline: starts stages via the Hermes execution port, collects artifacts, manages approval checkpoints, and records state transitions. Approval records are persisted via `backend/marketing/approval-store.ts`.

**Resumability rule:** Stages must preserve partial artifacts on rate-limit or transient gateway failures so a resume can pick up where it left off. Do not discard work on a non-fatal stage error — persist what completed, surface the failure, and let the orchestrator decide whether to retry. This rule exists because of past media-generation rate-limit incidents that lost completed creative on retry.

**Video execution rule:** Required video work must use the index-managed `skills/video-render-runtime/SKILL.md` contract. Aries supplies provider-neutral intent and ownership; Hermes owns provider and model selection.

### Auth & Tenant Model

Auth uses next-auth v5 (`auth.ts` at repo root) with Credentials + Google providers. Sessions are enriched with tenant claims (`tenantId`, `tenantSlug`, `role`) via JWT callbacks. `lib/tenant-context.ts` provides `getTenantContext()` which first checks session claims, then falls back to a DB lookup. Tenant roles: `tenant_admin`, `tenant_analyst`, `tenant_viewer`. All authenticated API routes should resolve tenant context server-side.

## Git & Worktree Discipline

- The first action of every task is `git fetch origin --prune`. Create branches and worktrees only from fresh `origin/master`, never a local ref; the creation command must contain the literal string `origin/`.
- Before editing, run `git rev-list --count HEAD..origin/master`. If the branch is more than 5 commits behind, stop and rebase or recreate it first.
- Immediately before pushing, re-fetch, `git rebase origin/master` (never merge master into a feature branch), confirm the base distance is 0, and run `npm run verify`. Push a rebased branch with `--force-with-lease`, never bare `--force`.
- Commit at logical checkpoints and never end a session with a dirty worktree; uncommitted work has no reflog and cannot be recovered.
- The reviewer who merges the PR removes its worktree, deletes the local branch, fetches with `--prune`, and runs `git worktree prune`.

## Build and Dev Commands

> **Turbopack is required.** `npm run dev` passes `--turbopack`; running `next dev` without it silently breaks Tailwind v4 styling. The `build` script does not pass the flag, so use `next build --turbopack` when invoking Next directly.

> **Pre-push gate:** `npm run verify` is the canonical fast regression suite and bakes in the env overrides tests need. Run it before every push.

```bash
# Install (force dev mode — system may have NODE_ENV=production)
NODE_ENV=development npm ci

# Dev server
npm run dev

# Type check
npm run typecheck

# Lint (typecheck + banned-pattern, repo-boundary, and protocol-drift checks)
npm run lint

# Quick regression suite (deterministic env overrides built in)
npm run verify

# Run all tests
npm run test

# Run a single test file
APP_BASE_URL=https://aries.example.com ./node_modules/.bin/tsx --test tests/some-test.test.ts

# E2E subset
npm run test:e2e

# Marketing flow tests specifically
npm run validate:marketing-flow

# Database init
npm run db:init

# Pre-flight check
npm run precheck
```

**Test environment notes:**
- Tests use Node.js built-in test runner via `tsx --test` (not Jest/Vitest).
- Many tests require `APP_BASE_URL=https://aries.example.com` to be set.
- On Windows, npm runs package scripts through `cmd`, so scripts beginning with inline environment assignments (`test`, `typecheck`, `lint`, and focused validators) do not parse. Run their underlying binaries with the same environment prefix from Git Bash; `npm run verify` and `npm run build` are Windows-safe.
- `npm run verify` wraps the fast regression suite with all needed env overrides and runs `npm run guardrails:agent` first.
- `npm run test:concurrent` runs the concurrency-sensitive regression set with `--test-concurrency=8`; use it before shipping work that changes routes, backend services, process management, or shared helpers.
- `npm run validate:execution-provider` is the focused Hermes submission, polling, and idempotent-ingestion gate.
- `npm run validate:social-content` is the focused weekly social-content gate.
- **Headless rendered-UI QA**: `scripts/qa/seed-qa-tenant.ts` + `scripts/qa/mint-qa-session.ts` mint a real Auth.js session for the passwordless `aries-qa-sandbox` bot (pinned identity, 12h TTL cap) so gstack `/browse` can screenshot/drive the live authenticated app from this host — see `docs/qa/headless-qa-sandbox.md`.

## Active operational guardrails

These rules are here because they already bit this repo.

1. **DB fan-out and endpoint latency:** Do not add `Promise.all` around PostgreSQL or gateway-backed call chains until you have checked `DB_POOL_MAX` and benchmarked the full endpoint, not just the helper. More parallel queries can make an isolated function faster while making the customer-facing request slower through pool contention. For production launch-scale, total database pressure = the app container (`ARIES_WEB_CONCURRENCY * DB_POOL_MAX`, plus 5 for the Hermes reconciler child pinned in `scripts/start-runtime.mjs`) + each sidecar worker's own `DB_POOL_MAX` (docker-compose explicitly sets 3 per sidecar — that compose value, not a universal default, is what keeps them small: the four dedicated worker pools fall back to 3 via `lib/db-pool-config.ts` when the env var is unset, but the insights-sync worker rides the shared `lib/db` pool whose unset fallback is 20; the honcho worker can additionally instantiate the shared pool through its backend imports, so budget 2× its value). `parsePoolMax` honors an explicit integer `DB_POOL_MAX` from 1 up to a cap of 200; anything else (`0`, `1e2`, `3garbage`) falls back to the caller's default with a warning — so compose values are the real per-process pool sizes.
2. **Parallel-agent duplicate work:** Before any agent ships or opens a PR from a parallel worktree, run `npm run guardrails:agent`. It performs `git fetch origin`, detects the base branch, compares `HEAD` to `origin/<base>`, and warns when the branch has no unique diff or looks like duplicate already-landed work.
3. **Codex worker prompt stalls:** If Codex or AO workers appear to run forever with no output, inspect tmux before assuming the task is hard. Codex can block on a model-upgrade prompt; select `Use existing model` with Down + Enter, then resume monitoring.
4. **Initial scale target:** Optimize the first production profile for about 50 people/users, not just a single happy-path demo. Use `ARIES_WEB_CONCURRENCY=4 DB_POOL_MAX=10` as the first container profile unless the database connection budget says otherwise, and validate with the 50-concurrent smoke check in `DOCKER.md`.
5. **Hermes is a POLLED API — delivery must be a standing process, never a per-request promise.** Hermes `/v1/runs` never invokes the submission's `callback_url`; Aries must poll runs to completion itself. The legacy in-process "poll-bridge" (`backend/marketing/ports/hermes.ts::runPollBridge`, still present as a best-effort fast path) was a `void this.runPollBridge(...)` fire-and-forget spawned by the submitting request — it did not survive the prod request lifecycle, so completed runs were never ingested and the stale-run reaper failed every marketing job (systemic outage, no success 2026-05-27 → fix). Durable delivery is the **Hermes reconciler** (`backend/marketing/hermes-reconciler.ts` + `scripts/hermes-reconciler-worker.ts`), spawned by `start-runtime.mjs` as a sibling of the Next.js cluster (same model as the stale-run reaper). It re-discovers in-flight marketing execution runs from disk every `ARIES_RECONCILER_INTERVAL_MS` (default 60s) and ingests finished ones via the same idempotent `handleHermesRunCallback` path (deterministic `event_id = reconcile-<hermesRunId>`). Gated by `ARIES_RECONCILER_ENABLED` (default ON). Reconciler delivery must always beat the reaper's tightest stage threshold (strategy 5 min). The reconciler **prevents** future loss; un-reaping historical `failed_stale` runs writes prod state and is a separate, sign-off-gated backfill (`hermes-callbacks.ts` drops callbacks for `state==='failed'` docs). Per-tick scan cost is bounded by an mtime window (`ARIES_RECONCILER_MAX_RECORD_AGE_MS`, default 24h) so it does not scale with all runs ever created; disk-level retention of old terminal `execution-runs/*.json` (nothing deletes them yet) remains a sign-off-gated follow-up (extend `hermes-kanban-gc-worker`).

## Key Directories

Non-obvious layout notes (the rest is discoverable by browsing):

- `backend/` vs `lib/` — `backend/` holds domain logic (marketing orchestrator, onboarding, execution port, approval store); `lib/` is for shared runtime helpers consumed by both `app/` and `backend/` (DB pool, auth, tenant context, runtime path resolution). Route handlers should import domain code from `backend/`, not inline it.
- `frontend/` vs `components/` — `frontend/` is screen-level and feature UI grouped by domain (`marketing/`, `social-content/`, `insights/`, `integrations/`, `onboarding/`, `settings/`, `aries-v1/`, etc.). Shared primitives live in `frontend/components/` and `components/redesign/`.
- `backend/execution/` — the Hermes execution surface (`provider-factory`, `providers/hermes`, `workflow-catalog`, `route-helpers`) consumed by route handlers; `backend/marketing/execution-port.ts` is the marketing-pipeline Hermes port.
- `specs/` — resolved via `lib/runtime-paths.ts`, not imported directly by path.
- `skills/` — marketing agent skill definitions (campaign-planner, creative-director, research, etc.) executed by the gateway, not TypeScript modules.
- `scripts/automations/` — holds nine long-lived sidecar workers, each self-scheduling via `setInterval` and each run as a single-replica `restart: unless-stopped` service in `docker-compose.yml` (the app-calling ones point `APP_BASE_URL` at the in-network `http://aries-app:3000` to skip the public DNS+TLS round-trip; no external cron required). The Deploy workflow force-recreates every one of these compose services onto the freshly built image after the app health check and then verifies each sidecar is running the target image ID (non-fatal, `::warning::` + step summary); `tests/deploy-manifest-parity.test.ts` (in `npm run verify` and CI) fails when a compose service is added without a matching force-recreate block in `.github/workflows/deploy.yml` — a worker omitted there keeps running a stale image across every deploy (the 2026-06-09 insights-sync incident):
  - `scheduled-posts-worker.mjs` — drains the `scheduled_posts` table and POSTs due rows to `/api/internal/publishing/scheduled-dispatch` every 60 seconds (`aries-scheduled-posts-worker` service). This is the publish back-half.
  - `weekly-job-trigger-worker.ts` — atomically claims a daily evaluation slot for each opted-in `marketing_schedule` row, then starts `weekly_social_content` through `POST /api/internal/marketing/weekly-trigger` only when that row's current weekly cadence window has no worker-created job (`aries-weekly-trigger-worker`, `tsx`-run). `day_of_week`/`hour_of_day`/`timezone` still define the weekly content window; only attempt frequency is daily, so an outage heals without replaying missed weeks. Gated by `ARIES_WEEKLY_TRIGGER_ENABLED` (default OFF — ships dormant); tick interval is `ARIES_WEEKLY_TRIGGER_INTERVAL_MS` (default 15 min). Cadence rows are managed with `scripts/marketing/upsert-marketing-schedule.ts`.
  - `draft-expiry-sweep-worker.ts` — expires STRANDED pre-publish posts (no `scheduled_posts` row, `published_at IS NULL`, `platform_post_id IS NULL` (never reached Meta), canonical `published_status` in `draft`/`in_review`/`approved`, `updated_at` older than the age window) by setting `published_status='expired'` (and the legacy `status` mirror), and emits the `aries_drafts_expiring_24h` warning count one day before rows cross that cutoff. Keys on the canonical `published_status`, NOT the legacy `status` mirror (which defaults to `'draft'` on Meta-native-scheduled posts → would false-positive-expire a live post). DB-only (no app round-trip); idempotent + batched; logic in `backend/marketing/draft-expiry-sweep.ts`. The `aries-draft-expiry-sweep-worker` service is `tsx`-run, gated by `ARIES_DRAFT_EXPIRY_ENABLED` (default OFF — ships dormant). It is the complement of the stale-run reaper, which reaps stranded *job docs on disk*; this sweep expires stranded *post rows in the DB*.
  - `insights-sync-worker.ts` — syncs platform analytics into the insights tables for every tenant with a connected `insights_accounts` row, every 30 minutes (`aries-insights-sync-worker` service, `tsx`-run). The overlap-guarded tick (`tickSafe`) releases its guard in `finally` and isolates per-tenant failures, so a failed tick (e.g. Postgres not up yet) retries next interval instead of wedging the worker (`tests/insights-sync-worker-tick-reset.test.ts`, in `npm run verify`). Each tick first sweeps `insights_sync_runs` rows stranded in `status='running'` (a SIGTERM mid-tick kills the dispatcher before its terminal UPDATE; no reaper covers this table) via `backend/insights/sync/sweep-stranded-runs.ts` — grace window `ARIES_INSIGHTS_SWEEP_GRACE_MINUTES` (default 60), served by the partial index `idx_insights_sync_runs_running_started`, sweep failures isolated from the tenant fan-out (`tests/insights-sync-worker-stranded-runs.test.ts` in `npm run verify`; live-schema proof in `tests/insights-sync-runs-sweep.requires-infra.test.ts`).
  - `honcho-performance-worker.ts` — delayed performance→Honcho memory leg: 24h–30d after publish it reads stored `insights_post_metrics_daily` snapshots (never fetches Meta) and writes `research_conclusion` events via `recordPerformanceEvent` (`aries-honcho-performance-worker` service, `tsx`-run; gated by `HONCHO_WRITE_PUBLISH_ENABLED`).
  - `gc-missing-hermes-assets-worker.ts` — marks `creative_assets` rows `orphaned_at` once their Hermes image-cache file has been evicted, so the dashboard stops emitting dead `/api/internal/hermes/media/<id>` URLs that 404 (the Posts-page preview-404 symptom, qa-defect #599). DB-only mark via `runGcMissingHermesAssets` (`scripts/gc-missing-hermes-assets.ts`); `queryProductionCreativeAssets` already filters `orphaned_at IS NULL`, so an orphaned row stops being surfaced (no URL emitted → no 404). Needs the read-only Hermes image-cache mount (`HERMES_IMAGE_CACHE_MOUNT`) to tell "evicted" from "unreadable" — without it the sweep is a fail-safe no-op. The `aries-hermes-gc-worker` service is `tsx`-run, gated by `ARIES_HERMES_GC_ENABLED` (default OFF — ships dormant). Only ever touches `storage_kind='runtime_asset'` rows older than the grace window (default 7d); `ingested_asset` uploads/composed stories are never touched; idempotent.
  - `usage-rollup-worker.ts` — AA-161 usage time-series layer: refreshes the hourly/daily/monthly rollups over `task_execution_log` (`backend/telemetry/usage-rollups.ts`) and then applies the retention policy (`backend/telemetry/usage-retention.ts`) every `ARIES_USAGE_ROLLUP_INTERVAL_MS` (default 1h). Postgres has no scheduler in this stack (no `pg_cron`, no Timescale), so "refresh the aggregates on a cadence" is a sidecar here. DB-only; `aries-usage-rollup-worker` service, `tsx`-run, gated by `ARIES_USAGE_ROLLUP_ENABLED` (default OFF — ships dormant), with retention behind its own second flag. Rolls up BEFORE it purges, and the purge refuses to touch anything at or above the rollup watermark.
  - `feedback-retry-sweep-worker.ts` — heals customer incident reports (SC-70 port, AA-51) whose Jira delivery is incomplete: parked `pending_retry` rows, attach-only completions, and stale `pending` rows stranded by a crash, via atomic `FOR UPDATE SKIP LOCKED` claims + the search-before-create idempotency label `aries-sub-<id>` (`backend/feedback/retry-sweep.ts`; `aries-feedback-retry-worker` service, `tsx`-run). Gated by `ARIES_FEEDBACK_RETRY_ENABLED` (default **ON** — unlike the other sidecars it is inherently dormant without `JIRA_*` config: one info line, no DB writes; default-on is what lets parked reports heal the moment config lands). Tick interval `FEEDBACK_RETRY_INTERVAL_MINUTES` (default 5).
  - `composio-connection-reconciler-worker.ts` — refreshes recently pending Composio connections until their provider state settles (`backend/integrations/composio/reconcile-pending-connections.ts`; `aries-composio-reconciler-worker` service, `tsx`-run). Gated by `ARIES_COMPOSIO_RECONCILER_ENABLED` (default OFF).
- `DOCKER.md` — container build/runtime profiles and the 50-concurrent smoke check referenced by guardrail #4.

## Tech Stack Notes

Most of the stack (React, Tailwind, Recharts, etc.) is discoverable from `package.json`. The non-obvious constraints:

- **Next.js 16** App Router with **Turbopack required** (Tailwind v4 breaks under webpack).
- **next-auth v5 beta** — session is enriched with tenant claims in JWT callbacks; do not read user info without going through `getTenantContext()`.
- **PostgreSQL** via `pg` pool in `lib/db.ts` — no ORM.
- **Imports use `@/*`** rooted at the repo (e.g. `@/backend/...`, `@/lib/...`).

## Commit & PR Conventions

This repo uses Conventional Commits with a scope (e.g. `fix(ci): ...`, `refactor(workspace): ...`, `feat(marketing): ...`). Match the existing style — `git log --oneline -20` is the source of truth. Keep subjects in the imperative and under ~70 chars; put detail in the body.

## Banned Patterns

`scripts/check-banned-patterns.mjs` enforces that certain strings never appear in key files. Banned terms include: `n8n`, `parity-stub`, `placeholder response/error`, `not yet wired`, `missing workflow wiring`, `intentionally disabled until`. Run `npm run validate:banned-patterns` to check.

## Deploy Configuration

- platform: docker (self-hosted via GitHub Actions Deploy workflow)
- production.url: https://aries.sugarandleather.com
- deploy.workflow: .github/workflows/deploy.yml
- health.check: curl -sf https://aries.sugarandleather.com -o /dev/null -w "%{http_code}"

## Environment Variables

Required for Hermes-native local/runtime setup: `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `APP_BASE_URL`, `HERMES_GATEWAY_URL`, `HERMES_API_SERVER_KEY`, `INTERNAL_API_SECRET`, `NEXTAUTH_URL`, `AUTH_URL`, `NEXTAUTH_SECRET`

Optional safety flags:
- `ARIES_MEMORY_LABEL_REDACTION_V2=1` — switches `scrubPreferenceLabelForHoncho` (`backend/memory/write-events.ts`) from the legacy broad `[A-Z][a-z]+\s+[A-Z][a-z]+` regex to a narrow first-name-denylist heuristic. Preserves creative descriptors like "Bold Minimalist" / "Quiet Luxury" while still scrubbing `<FirstName> <LastName>` pairs. **Shipped default is ON** — `docker-compose.yml` pins `ARIES_MEMORY_LABEL_REDACTION_V2:-1` (v2 narrow heuristic), so the live container runs v2; the *code* default when the env var is entirely unset (e.g. some tests) is the legacy v1 broad regex. Set `0` to force legacy v1. Email redaction is unchanged in both modes.
- `ARIES_AUTO_APPROVE_MARKETING_PIPELINE=1` — when Hermes emits `requires_approval` for strategy/production/publish, Aries synthesizes an `ai-orchestrator` approval from the callback handler and resumes the pipeline without a human click. Closes the autonomous E2E loop. Process-wide — affects ALL tenants in this container. Default ON in `docker-compose.yml` (single-tenant prod, autonomous mode); set to `0` to require human approval clicks again.
- `ARIES_BRAND_ENRICHMENT_ENABLED=1` — enables LLM-backed brand-kit enrichment via Hermes (`extractEnrichAndSaveTenantBrandKit`). Persists `positioning`, `audience`, `tone_of_voice`, and `style_vibe` to `brand-kit.json`; wires them into the weekly Hermes payload. Default ON in `docker-compose.yml` (single-tenant prod). Set to `0` to disable enrichment and use scraped-only brand kit.
- `ARIES_SOCIAL_COPY_FINALIZE_ENABLED=1` — enables the post-creative `social_copy_finalize` stage after image approval. Aries treats `1`, `true`, `yes`, or `on` as enabled; when the flag is unset/off, the orchestrator skips `social_copy_finalize`, marks it skipped, and continues with the legacy publish path. Ship this flag default OFF and leave it OFF until the Hermes-side `social_copy_finalize` workflow is registered and quality-evaluated; Hermes workflow registration itself remains out of scope for Aries.
- `ARIES_VIDEO_PUBLISH_ENABLED=1` — rollout switch for video / Reel / Story video publishing to Facebook + Instagram. Aries treats `1`, `true`, `yes`, or `on` as enabled. Default OFF. When OFF, `synthesizePublishPostsFromContentPackage` strips schedule entries whose `placement` is `reel` or whose `media_type` is `video` (a reel has no image fallback, so that post/platform target is skipped); image/feed posts are unaffected so the campaign still succeeds. When ON, video/Reel/Story-video entries persist with the new `surface` (`feed`/`story`/`reel`) + `media_type` axes and dispatch through the new Meta Graph video branches (IG `media_type=VIDEO|REELS|STORIES` container + extended poll; FB `/videos` file_url + `/video_stories` start/finish). Per-surface media validation (`backend/integrations/meta-media-validation.ts`) is metadata-driven from Hermes width/height/duration and fails closed. Leave OFF until the Hermes side emits `media_type:"video"` + per-asset video metadata (separate repo).
- `ARIES_ONBOARDING_VARIANT_BOARD_ENABLED=1` — rollout switch for the first-post onboarding variant board → taste profile (`docs/plans/2026-06-02-onboarding-variant-board.md`). Aries treats `1`, `true`, `yes`, or `on` as enabled (`isOnboardingVariantBoardEnabled`, `backend/onboarding/variant-board-env.ts`). Default OFF. When OFF, onboarding runs the existing single weekly job with no board (byte-identical to today). When ON, a new user's first post is generated as a 3-variant board (Aries fans out 3 `submitRawRun` Hermes runs — no Hermes contract change); the user picks one, rates 1-5, and can regenerate / more-like-this / freeform-edit; the pick + ratings + edits write taste to BOTH the new Aries `marketing_taste_profile` table (read-time bias, 5%/week decay computed at read in `backend/marketing/taste-profile-store.ts`) AND Honcho (durable, via `recordOnboardingVariantTasteSignalEvent` — the Honcho leg is still additionally gated by `HONCHO_WRITE_PREFERENCES_ENABLED`), and the remaining week-1 posts are anchored to the chosen variant. Process-wide (all tenants in the container). Keep OFF until the rendered onboarding/dashboard is screenshot-verified per the plan's success bar.
- `ARIES_TEST_REQUIRES_INFRA_ENABLED=1` — opt-in switch for running the requires-infra (live-Postgres) test split locally. Aries treats `1`, `true`, `yes`, or `on` as enabled. Default OFF. When OFF, the live-DB test files (indexed in `tests/REQUIRES_INFRA.md`) self-skip with `t.skip('database env not configured')` via the shared `requireDbEnvOrSkip` guard (`tests/helpers/requires-infra.ts`), exactly as the `full-suite` CI gate expects. When ON **and** `DB_HOST`/`DB_PORT`/`DB_USER`/`DB_PASSWORD`/`DB_NAME` point at a reachable Postgres, `npm run test:requires-infra` runs those files for real. This flag is read **only by the test harness, never by the app runtime, and never set by CI** — so it lives here + `.env.example` and is deliberately kept out of `docker-compose.yml`. `npm run test:requires-infra-report` prints the self-contained vs requires-infra split without running anything.
- `ARIES_AUTOSCHEDULE_ON_APPROVAL=1` — the **safe alternative to `ARIES_AUTO_APPROVE_MARKETING_PIPELINE`**. Aries treats `1`, `true`, `yes`, or `on` as enabled (`autoScheduleOnApprovalEnabled`, `backend/marketing/hermes-callbacks.ts`). Default OFF. When OFF, a job's already-approved posts only auto-schedule under the no-review autonomous flag, so with approval-gating on (the safe prod setting) approved posts strand. When ON, once a **human** approves the publish gate, the week's posts auto-schedule to both Instagram and Facebook. The hook is one guard at the single completion convergence point (`synthesizePublishPostsOnCompletion` in `backend/marketing/hermes-callbacks.ts`), so it is correct for human-approve, auto-approve, multi-stage, and reconciler-delivered completions, fires once per terminal callback, and is idempotent on re-delivery (`upsertScheduledPost ON CONFLICT(post_id)`). Default OFF in `docker-compose.yml` (`ARIES_AUTOSCHEDULE_ON_APPROVAL:-0`).
- `ARIES_SYNTHESIZE_ON_PUBLISH_SKIP_ENABLED=1` — synthesize reviewable `posts` on the **publish-SKIP** terminal path (`isSynthesizeOnPublishSkipEnabled`, `backend/marketing/synthesize-on-publish-skip-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. A weekly/social job whose runtime `publishingRequested` is false (the default for **every** `weekly_social_content` job — `orchestrator.ts` only sets it true for one-off jobs; `requestedPublishFlag` reads `inputs.request.{publishRequested,livePublishPlatforms}`, which the weekly create-form never sets) goes terminal straight from the production `approve_publish` callback. That path ingests the rendered images into `creative_assets` but **never synthesizes `posts`**, so the operator is left with images and NO captions/hashtags and NO "Publish"/"Approve" control anywhere (the publish queue + review queue both read from synthesized posts → the "generated images but nowhere to click to publish" symptom; the copy is generated and sits in `production.primary_output.content_package`, just never surfaced). When ON, the publish-skip path also calls `synthesizePublishPostsOnCompletion(doc, null, { autoSchedule: false })` so the copy lands as `approved` `posts` that surface with a manual **"Publish now → Publish to Facebook Page"** button. **`autoSchedule:false` is the safety contract:** it synthesizes for review only and **never auto-schedules/auto-publishes — even with `ARIES_AUTO_APPROVE_MARKETING_PIPELINE` / `ARIES_AUTOSCHEDULE_ON_APPROVAL` on** (the auto-schedule lives only inside that same helper, behind the param) — because publishing was not requested; the human still clicks publish. Runs AFTER the creative_asset ingest so each post links to its image; best-effort/non-fatal. Onboarding **variant-board** jobs awaiting a pick are skipped (`!isVariantBoardJobAwaitingPick`) so a held/unpicked variant never becomes a publishable post. When OFF (default) the publish-skip path is byte-identical to today. Process-wide; default OFF in `docker-compose.yml` (`ARIES_SYNTHESIZE_ON_PUBLISH_SKIP_ENABLED:-0`). **Interaction with the live draft-expiry sweep:** these synthesized posts are `published_status='approved'` with no `scheduled_posts` row, so — like ANY approved-but-unpublished post — they match the `aries-draft-expiry-sweep-worker` predicate and are set to `expired` once `updated_at` passes `ARIES_DRAFT_EXPIRY_AGE_DAYS` (default 14d, sweep live in prod since 2026-06-08). So the operator has the age window to click Publish before a stranded reviewable post is swept; widen `ARIES_DRAFT_EXPIRY_AGE_DAYS` if a longer review runway is wanted. **Screenshot-verify a freshly generated weekly post shows the Publish button on a live tenant before flipping in prod.**
- `ARIES_WEEKLY_TRIGGER_ENABLED=1` — rollout switch for weekly content with daily recovery (`docs/plans/2026-06-04-weekly-social-content-automation.md`). Aries treats `1`, `true`, `yes`, or `on` as enabled (`weeklyTriggerEnabled`, `scripts/automations/weekly-job-trigger-worker.ts`). Default OFF. When ON, the worker atomically claims one evaluation per tenant-local day at the configured hour and passes the current `marketing_schedule` weekly boundary to `POST /api/internal/marketing/weekly-trigger`. Route-level window idempotency starts a job only if that cadence window lacks one; a three-week outage therefore creates only current-window content. Failed submits revert the daily claim, while successful/deduped/skipped attempts retry the following day. `day_of_week`/`hour_of_day`/`timezone` retain cadence semantics; the CLI remains `scripts/marketing/upsert-marketing-schedule.ts`. Default OFF in `docker-compose.yml` (`ARIES_WEEKLY_TRIGGER_ENABLED:-0`).
- `ARIES_WEEKLY_TRIGGER_INTERVAL_MS` — tick interval (ms) for the `aries-weekly-trigger-worker` self-scheduling loop. Default `900000` (15 minutes); a non-positive or unparseable value falls back to the default. Set in `docker-compose.yml` (`ARIES_WEEKLY_TRIGGER_INTERVAL_MS:-900000`). Only meaningful when `ARIES_WEEKLY_TRIGGER_ENABLED` is on.
- `ARIES_WEEKLY_TRIGGER_SUBMIT_TIMEOUT_MS` — AbortSignal timeout (ms) on the weekly-trigger worker's submit POST to `/api/internal/marketing/weekly-trigger`. Default `600000` (10 min — generous, the route owns the Hermes submission); a non-positive or unparseable value falls back. Without it a hung submit (wedged Hermes gateway) held the claim stamped and silently skipped the whole week (2026-07-20 incident). On timeout the claim reverts and the next tick retries; a submit that landed server-side anyway is collapsed by the route's idempotency guard. Pairs with the `marketing_weekly_claims` in-flight marker (written atomically with the claim, released on every concluded outcome): each tick heals markers older than max(3× this timeout, 45 min) by reverting the stranded claim, so a process kill mid-attempt can no longer lose the week either. Set in `docker-compose.yml` (`ARIES_WEEKLY_TRIGGER_SUBMIT_TIMEOUT_MS:-600000`).
- `ARIES_DUPLICATE_CAPTION_WINDOW_DAYS` / `ARIES_SAME_PLATFORM_MIN_SPACING_MINUTES` — the unattended-publish guards on the scheduled-dispatch route (`resolvePublishGuards`, `app/api/internal/publishing/scheduled-dispatch/route.ts`; 2026-07-13 duplicate-post incident). Duplicate-caption: a post whose exact trimmed caption (≥20 chars) already published to the same tenant+platform within N days is **terminally** blocked (`duplicate_content_blocked`, non-retryable). Spacing: a publish younger than M minutes on the same tenant+platform **defers** the dispatch (retryable — the worker re-claims later), making an N-posts-at-one-instant same-platform burst structurally impossible at the publish boundary regardless of upstream scheduling bugs (and keeping Meta's spam heuristics, e.g. FB error 368, from tripping). Defaults 14 / 30; `0` disables each independently; both guards **fail open** on any DB error (a guard outage never stops legitimate delivery). Manual "Publish now" uses a different route and is deliberately unguarded (human intent). Wired into the `aries-app` service env block in `docker-compose.yml`.
- `ARIES_DISPATCH_RETRY_BACKOFF_MINUTES` / `ARIES_DISPATCH_RATE_LIMIT_BACKOFF_MINUTES` / `ARIES_DISPATCH_TRANSIENT_MAX_ATTEMPTS` — scheduled-dispatch policy defaults 10 minutes / 180 minutes / 5 attempts. Failures are persisted as `auth_token`, `platform_transient`, `platform_permanent`, or `media_invalid`: only platform-transient retries, with the longer tier for rate-limit signatures; terminal classes and exhausted transients enter `dead_letter` with `dead_lettered_at`, increment `aries_dispatch_dead_letters_total`, and are never reclaimed automatically. Outcome-unknown remains the stricter `manual_reconciliation` quarantine because the post may already be live. Schema ships in `scripts/init-db.js` + `migrations/20260806000000_scheduled_dispatch_dead_letters.sql`; staged alerts and replay steps live under `ops/alerts` and `docs/runbooks`.
- `ARIES_REAPER_AWAITING_APPROVAL_THRESHOLD_MS` — the window (ms) before the stale-run reaper reaps a job that is correctly *waiting for a human* at an approval gate. Default `604800000` (7 days); previously such jobs were reaped at the 5-minute strategy stage threshold, which broke human-in-the-loop approval (`DEFAULT_AWAITING_APPROVAL_THRESHOLD_MS`, `backend/marketing/stale-run-reaper.ts`). Jobs are still reaped eventually so a genuinely wedged gate is caught, with a loud log. An explicit force-reap override (CLI `--threshold-ms` or the `STALE_RUN_REAPER_THRESHOLD_MS` env var) still wins over this awaiting-approval window.
- `ARIES_DRAFT_EXPIRY_ENABLED=1` — rollout switch for the draft-expiry sweep (`aries-draft-expiry-sweep-worker` / `backend/marketing/draft-expiry-sweep.ts`). Aries treats `1`, `true`, `yes`, or `on` as enabled (`draftExpiryEnabled`). Default OFF. When OFF, the worker exits-to-idle (no restart loop, no DB writes) and ships dormant. When ON, every tick it expires STRANDED pre-publish posts — a post with **no** `scheduled_posts` row (never reached the publish queue), `published_at IS NULL` (never went live), `platform_post_id IS NULL` (never reached Meta), a canonical `published_status` of `draft`/`in_review`/`approved`, and `updated_at` older than the age window — by setting `published_status='expired'` + `status='expired'` + `expired_at=now()`. It keys on the canonical `published_status` (not the legacy `status` mirror, which defaults to `'draft'` on Meta-native-scheduled posts and would otherwise false-positive-expire a live post). This removes them from the operator's approval/backlog trays **without publishing stale content**, stopping the unscheduled-approved backlog (the "36 stranded approved IG posts" symptom) from growing once the weekly trigger fans out. It is the DB-row complement of the stale-run reaper (which reaps stranded job docs on disk). Idempotent (an expired post no longer matches the predicate), batched, and every mutating statement re-checks the full predicate so a post that gets scheduled/published mid-sweep is skipped. **Before first enabling in prod, run one cycle with `ARIES_DRAFT_EXPIRY_DRY_RUN=1`** to observe candidate counts read-only, then flip it back to commit. Default OFF in `docker-compose.yml` (`ARIES_DRAFT_EXPIRY_ENABLED:-0`). Requires the `'expired'` status value + `expired_at` column from `scripts/init-db.js` (applied on container start).
- `ARIES_DRAFT_EXPIRY_DRY_RUN=1` — when truthy, every draft-expiry tick runs read-only: it counts candidates (total + per-tenant) and logs them but mutates nothing. The safe first step when enabling the sweep in prod (flip `ARIES_DRAFT_EXPIRY_ENABLED=1` + `ARIES_DRAFT_EXPIRY_DRY_RUN=1` for one observation cycle, then set `DRY_RUN=0`). Default OFF (the sweep commits when enabled, matching the other workers). Only meaningful when `ARIES_DRAFT_EXPIRY_ENABLED` is on.
- `ARIES_DRAFT_EXPIRY_AGE_DAYS` — how many days a pre-publish post must sit untouched (`updated_at < now() - age`) before it is eligible to expire. Default `14`; a non-positive or unparseable value falls back. A generous window so a post actively being reviewed is never expired out from under the operator. Only meaningful when `ARIES_DRAFT_EXPIRY_ENABLED` is on.
- `ARIES_DRAFT_EXPIRY_INTERVAL_MS` — tick interval (ms) for the `aries-draft-expiry-sweep-worker` self-scheduling loop. Default `21600000` (6 hours); a non-positive or unparseable value falls back to the default. Only meaningful when `ARIES_DRAFT_EXPIRY_ENABLED` is on.
- `ARIES_HERMES_GC_ENABLED=1` — rollout switch for the missing-Hermes-asset GC sweep (`aries-hermes-gc-worker` / `scripts/gc-missing-hermes-assets.ts`'s `runGcMissingHermesAssets`). Aries treats `1`/`true`/`yes`/`on` as enabled (`gcEnabled`). Default OFF. When OFF the worker idles dormant (no restart loop, no DB writes). When ON, every tick it marks `creative_assets.orphaned_at = now()` for `storage_kind='runtime_asset'` rows whose file is gone from the read-only Hermes cache mount **and** older than the grace window — so the dashboard stops emitting dead `/api/internal/hermes/media/<id>` URLs that 404 (qa-defect #599; `queryProductionCreativeAssets` already filters `orphaned_at IS NULL`). It is the DB-row complement of the media route's 404 + the UI's "Preview archived" fallback. Composed stories / operator uploads (`ingested_asset`, persisted under `DATA_ROOT`) are **never** touched (the `SELECT` and the `UPDATE` both re-assert `storage_kind='runtime_asset'`). Idempotent (an orphaned row leaves the predicate). **Before first enabling in prod, run one cycle with `ARIES_HERMES_GC_DRY_RUN=1`** to observe candidates read-only, then flip it back to commit. Requires the read-only Hermes image-cache mount (`HERMES_IMAGE_CACHE_MOUNT`); without it the sweep is a fail-safe no-op. Default OFF in `docker-compose.yml` (`ARIES_HERMES_GC_ENABLED:-0`).
- `ARIES_HERMES_GC_DRY_RUN=1` — when truthy, every GC tick runs read-only: it lists orphan candidates and logs them but mutates nothing. The safe first step when enabling the sweep in prod (flip `ARIES_HERMES_GC_ENABLED=1` + `ARIES_HERMES_GC_DRY_RUN=1` for one observation cycle, then set `DRY_RUN=0`). Default OFF. Only meaningful when `ARIES_HERMES_GC_ENABLED` is on.
- `ARIES_HERMES_GC_MAX_AGE_DAYS` — grace window (days) a missing-file `runtime_asset` row must exceed (`created_at < now() - age`) before it is orphaned, so a freshly-generated asset Hermes may still be writing or will regenerate on a retry is never prematurely orphaned. Default `7`; a non-positive or unparseable value falls back. Keep it ≥ the Hermes regenerate/retry window. Only meaningful when `ARIES_HERMES_GC_ENABLED` is on.
- `ARIES_HERMES_GC_INTERVAL_MS` — tick interval (ms) for the `aries-hermes-gc-worker` self-scheduling loop. Default `21600000` (6 hours); a non-positive or unparseable value falls back to the default. Only meaningful when `ARIES_HERMES_GC_ENABLED` is on.
- `ARIES_INSIGHTS_SWEEP_GRACE_MINUTES` — grace window (minutes) before the insights-sync worker's per-tick stranded-run sweep (`backend/insights/sync/sweep-stranded-runs.ts`) fails out an `insights_sync_runs` row stuck in `status='running'` with `error_message='aborted by worker restart'`. Default `60`; a non-positive, fractional, or unparseable value falls back. Widen before shipping any sync path that can legitimately run longer than an hour (e.g. a backfill script). A swept run that later completes still converges: the dispatcher's terminal UPDATEs key on `id` alone and the ok path clears `error_message`.
- `ARIES_INSIGHTS_ATTRIBUTION_SCOPE_ENABLED=1` — rollout switch for the S4-1/AA-104 attribution scope (`isAttributionScopeEnabled`, `backend/insights/attribution-scope-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. **Default OFF.** When OFF, `resolveAttributionScope` returns the all-channel result **without running the coverage query at all**, so Activity and Top produce exactly today's numbers at exactly today's DB cost. **Do not flip this on until every post-derived section moves to the same scope.** Six builders read `insights_posts` (activity, attention, conversations, goal, narrative, top, trends) and two pairs share a number across that boundary: (1) `hoursSaved` is deliberately computed from ONE helper (`estimateHoursSaved`, `backend/insights/hours-saved.ts`) so the Hero band and the Activity strip can never show two different numbers (S3-1/AA-97, commit `2eb0a8ce`) — the Hero's post count is unscoped, so a tenant above the threshold but below 100% coverage would see them disagree again; and (2) Activity's high-performer count "mirrors the Section 3 detection so both sections agree" (commit `0040630e`), but Section 3 (`attention-snapshot-builder.ts`) still computes its ≥2× baseline over every post, so a scoped Activity baseline makes the same post a high performer in one section and not the other. Section 3's "View details" CTA also scrolls straight to Section 6's `#top-performing` anchor, so an unscoped Section 3 could name a top post that a scoped Section 6 omits. A coherent attribution view is a dashboard-wide change, not a two-section one; this flag ships the decision, the coverage gate and the honest scope label now and keeps the numbers on the all-channel basis until the rest follows. Process-wide; default OFF in `docker-compose.yml`.
- `ARIES_INSIGHTS_ATTRIBUTION_THRESHOLD` — the S4-1/AA-104 attribution scope for the Activity and Top insights sections (`resolveAttributionScope`, `backend/insights/attribution-scope.ts`). Only meaningful when `ARIES_INSIGHTS_ATTRIBUTION_SCOPE_ENABLED` is on. Share (0–1) of a period window's `insights_posts` rows that must carry `aries_post_id` before both sections scope their queries to **Aries-published** posts; below it they read **all channel** posts. Default `0.8`; a value outside the inclusive 0–1 range, or one that does not parse, falls back to the default with a warning (a typo'd threshold must not silently scope a section to nothing). **Why the fallback is mandatory:** #785 removed the `aries_post_id IS NOT NULL` filter from these two sections because nothing wrote the column, so every tenant saw empty sections; S3-3 shipped the writer + backfill, and re-adding the filter unconditionally would reproduce that regression for any tenant with unstamped history. Scoping requires BOTH `coverage >= threshold` **and** `attributedPosts > 0` — the second half is load-bearing, not redundant: `computeAttributionCoverage` reports an all-unattributed window as trustworthy at threshold `0`, and scoping there would empty a populated section. Above a positive threshold the attributed set is at least `threshold x totalPosts` of a non-empty set, so the scoped section is non-empty **by construction** (pinned across a threshold/count grid in `tests/insights-attribution-scope.test.ts`). The decision is one extra sequential COUNT per section request on the window the builder already resolved (no fan-out, guardrail #1), and **fails open**: a coverage-query error, impossible counts, or unparseable rows all degrade to all-channel, never to an empty section or a thrown request. The resolved scope rides the response as `meta.attribution` (`scope`, `attributedOnly`, `totalPosts`, `attributedPosts`, `coverage`, `threshold`) and the dashboard labels each section with it ("Aries-published posts" vs "all channel activity") so the number on screen always says which set it counts. Scope is a property of the **data**, not the request, so it is deliberately **not** part of the cache key: a tenant that crosses the threshold mid-cache keeps serving the all-channel body until the 1h TTL expires. Bumped `TEMPLATE_VERSION` to `activity-v7` / `top-v8` — every strip number, the content mix, the ranked post list and the period average reach (hence every multiplier derived from it) change for a backfilled tenant. Process-wide; wired into the `aries-app` service `environment:` block in `docker-compose.yml`.
- `ARIES_INSIGHTS_FORCE_THROTTLE_ENABLED` / `ARIES_INSIGHTS_FORCE_THROTTLE_BURST` / `ARIES_INSIGHTS_FORCE_THROTTLE_WINDOW_MS` — the S7-2/AA-120 bound on the authenticated `?force=true` cache bypass for the six cached insights sections (`backend/insights/force-throttle.ts`; narrative, goal, attention, activity, trends, top). **Shipped default is ON** (`docker-compose.yml` pins `:-1`, and the *code* default with the var entirely unset is also enabled) — deliberately unlike most flags here, because a rate limit that ships disabled protects nothing; this is the kill switch, not the on switch. Only an explicit `0`/`false`/`no`/`off` disables it, so a typo fails safe (still limited) rather than silently reopening the bypass. **What it actually protects:** all six handlers acquire a pooled client *before* they branch on `force`, so an unthrottled forced request costs a held connection for the whole rebuild plus the builder's queries and an upsert. AA-122's singleflight already collapses *concurrent* forced builds (its key has no `force` component), but two costs survive that collapse — the connection each waiter holds (with `DB_POOL_MAX` 10 per worker a burst starves every other endpoint) and the per-follower `INSERT ... ON CONFLICT DO UPDATE` — and singleflight does nothing at all for *serial* hammering, where each sequential force runs a complete builder. **The gate therefore runs before `pool.connect()`** (pinned by a source-level assertion in `tests/insights-force-throttle.test.ts`): a throttle placed after the client is acquired would concede the exact resource it exists to protect. Token bucket per `(tenant, section)` — per-section granularity is load-bearing, since one bucket per tenant would let retries on a broken Trends panel lock the user out of retrying a broken Goal panel. Denied requests get a `429 {status:'error', error:'rate_limited', retry_after_ms}` plus a `Retry-After` header (whole seconds, rounded **up**), matching the shape `app/api/feedback/route.ts` already established. **Why the default allowance is generous (5 per section per 5 min):** `force=true` is reachable from the browser through exactly one affordance — the Retry button inside `<ErrorState>` (`useInsight`'s `refetch` is `run(true)`; there is no Refresh control, nothing polls these six endpoints, and the hook never auto-retries, so a 429 cannot cause a retry storm). The only human who ever forces is one whose section already failed, and throttling them throttles the sole recovery path in the UI — so the limit is set where a person mashing Retry never reaches it while a 100 req/s script is cut to 5 rebuilds per 5 minutes. Hammering while throttled cannot push recovery further out (tokens are recomputed from elapsed time, never reset on a denial). `useInsight` special-cases 429 with a real cooldown message instead of a bare “Server error (429)” and never clears `data`, so a throttled refresh leaves a populated panel intact. State is **per-process**, matching the precedent `cache-policy.ts` set for its in-flight map — and the argument is stronger here, since a cross-process counter would have to be read from Postgres on the very path under contention; with `ARIES_WEB_CONCURRENCY` workers (2 in compose, 4 per guardrail #4) the real ceiling is that multiple of the burst. Burst/window fall back to defaults on non-positive, non-integer or unparseable values — note `1e3` is rejected rather than read as 1 (the `parsePoolMax` trap). Note these six endpoints check no role, so `tenant_viewer` can force too. Process-wide; wired into the `aries-app` service `environment:` block.

- `ARIES_COMMENT_CLASSIFICATION_ENABLED=1` — rollout switch for comment sentiment/lead/category classification (`isCommentClassificationEnabled`, `backend/insights/sync/classify-comments.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. **Shipped default is ON** — `docker-compose.yml` pins `ARIES_COMMENT_CLASSIFICATION_ENABLED:-1` on the `aries-insights-sync-worker` service (2026-07-16, roadmap flag-flip row 1/S1-11); the *code* default when the env var is entirely unset (e.g. some tests) is OFF. Set `0` to disable. Without it (or without Hermes gateway creds) comments are still fetched into `insights_comments`, but `insights_comment_classifications` stays empty — so Conversations shows `0% positive` + an empty "What people are asking" panel and the goal `lead_generation` count reads 0 (there is otherwise **no** production classifier; only the demo seed writes labels). When ON, the insights-sync dispatcher's comment leg sends one bounded batch (`MAX_CLASSIFY_BATCH`, 40) of that account's unclassified recent comments to a **raw Hermes run** (the same submit-`/v1/runs`+poll pattern as `brand-kit-enrich.ts` — a prompt run with a model hint via `instructions`, **no pre-registered Hermes skill**, so no Hermes-repo change) and stores `{sentiment ∈ positive|neutral|negative, is_lead, category ∈ question|compliment|complaint|spam|other}` per comment (`classifier_version='hermes-comment-v1'`). Bounded to one batch per account per tick so it converges over ticks without extending the tick unboundedly. Best-effort + isolated to `legErrors`: a classification outage never breaks the sync (the run downgrades to `partial`, the other legs still persist). Requires `HERMES_GATEWAY_URL` + `HERMES_API_SERVER_KEY` in the `aries-insights-sync-worker` service env (wired in `docker-compose.yml`); `HERMES_COMMENT_CLASSIFY_MODEL` optionally overrides the model hint (default `gemini/gemini-3-flash-preview`). The output vocabulary is pinned to what `conversations-builder.ts::deriveTag` + the goal lead count consume. **Empty-default trap (deliberate):** with the flag ON but Hermes creds missing in the worker env, any sync run that attempts classification (i.e. has unclassified comments in its batch window) reports `not_configured` into `legErrors` and downgrades to `partial` — misconfiguration is loud, not silent; the other legs still persist, and runs with nothing to classify stay `ok`. Process-wide; shipped default ON in `docker-compose.yml` (`ARIES_COMMENT_CLASSIFICATION_ENABLED:-1`).
- `ARIES_FEED_LOGO_COMPOSITE_ENABLED=1` — rollout switch for compositing the **real** brand logo onto generated single-image FEED creatives (`isFeedLogoCompositeEnabled`, `backend/social-content/feed-logo-composite-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF, ingest is byte-identical to today and the image brief keeps its `Brand logo: <url>` instruction (the model draws a logo). When ON: (1) `ingestProductionCreativeAssetsToDb` composites the brand kit's materialized logo onto eligible single-image feed assets via `applyBrandFrame` (`backend/creative-memory/frame-overlay.ts`, border-off + a luminance-sampled conditional feathered scrim so the light mark reads on bright photo regions), **replacing** the raw row's bytes (one row, `storage_kind='ingested_asset'`, framed checksum, self-referential `served_asset_ref` — so IG publish posts the framed image; the #555 surface); and (2) `buildProductionResumeContext` drops the `Brand logo:` line and tells the model **not** to draw a logo (else the final image carries two). Requires the brand kit's `logo_file_path` (downloaded during brand-kit enrichment; backfill existing tenants with `scripts/marketing/materialize-tenant-logo.ts --tenant N` / `--all` / `--logo-file <path>`). Framing is best-effort/non-fatal: any failure falls back to the raw bytes so publish is never blocked. Process-wide; default OFF in `docker-compose.yml`. **Screenshot-verify a freshly generated + published post on a live tenant before flipping in prod** (only rendered output counts).
- `ARIES_TASTE_BRIEF_INJECTION_ENABLED=1` — rollout switch for the brand taste-learning READ path (PR2 Phase 2; `isTasteBriefInjectionEnabled`, `backend/marketing/taste-brief-injection-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When ON, the Hermes marketing port preloads `loadTasteForBriefByTenant(tenantId)` (the decayed **tenant-scoped** taste row — `marketing_taste_profile` `user_id IS NULL`, the userless weekly run has no `users.id`) async in `invoke` (fail-open `.catch(()=>null)`), threads it through `submissionPayload` into `buildProductionResumeContext`, and folds high-confidence descriptors into the per-image brief (`style_descriptors`→Style and vibe, `voice_descriptors`→Brand voice, `avoid`→Must avoid, `audience_descriptors`→a learned-audience line). When OFF (default) or the projection is null/empty, the brief is **byte-identical** to today (golden-tested). Pairs with the `20260609_marketing_taste_tenant_scoped` migration (relaxes the taste tables' `user_id NOT NULL` + PK). Process-wide; default OFF in `docker-compose.yml`. Reads an empty profile until `ARIES_POST_EDIT_TASTE_LEARNING_ENABLED` has accumulated signal, so turn learning ON first.
- `ARIES_POST_EDIT_TASTE_LEARNING_ENABLED=1` — rollout switch for the brand taste-learning WRITE path (PR2 Phase 3; `isPostEditTasteLearningEnabled`, `backend/marketing/post-edit-taste-learning-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When ON: (1) `synthesizePublishPostsFromContentPackage` stamps a stable visual-style lens (`style_dimension='visual_style'`, `style_value=<brand style_vibe>`) on each synthesized `posts` row; and (2) three best-effort, non-fatal producers write a **tenant-scoped** taste signal (`applyTenantTasteSignal`, `backend/marketing/review-edit-taste.ts`) mapping structural operator actions onto that lens with **no LLM** — a creative **review-tray approve → approved**, a **review-reject/changes_requested → rejected** (`recordMarketingReviewDecision`), a **creative regenerate → rejected** (`regenerateCreativeAsNewRun`), and a **post delete → rejected** (reads the deleted row's stamp). When OFF (default) nothing is stamped (NULL columns) and no taste is written — synthesized rows + edit routes are byte-identical to today. Every producer is wrapped (a taste write never breaks the operator action) and a no-op when the flag is OFF. v1 deliberately excludes caption-edit/brief-PATCH (ambiguous), upload-replace (redundant with regenerate), and the job-level approve handler (covered by per-item review approve; avoids reconciler re-delivery over-count). Needs the `20260609` taste-scope migration (`style_dimension`/`style_value` columns + relaxed taste tables). Process-wide; default OFF in `docker-compose.yml`.
- `ARIES_SLACK_NOTIFICATIONS_ENABLED=1` — rollout switch for **outbound** Slack approval notifications (Phase 4 PR2; `isSlackNotificationsEnabled`, `backend/integrations/slack/notify-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. This gates only the new **outbound** direction; the inbound Events webhook (PR1, `app/api/integrations/slack/events`) is unaffected. When ON, the marketing Hermes callback posts a Block Kit "needs approval" message with a **Review in Aries** deep link (`/social-content/review?jobId=…`) whenever a job stage is paused for a **human** to approve — fired at the single `requires_approval` checkpoint convergence point (`createApprovalCheckpoint` in `backend/marketing/hermes-callbacks.ts`), but **only after** `maybeAutoApproveMarketingCheckpoint` has had its turn, so an auto-approved gate is never announced as if a human must act (gate: `doc.stages[stage].status === 'awaiting_approval'`). Best-effort + non-fatal (`backend/integrations/slack/client.ts` never throws; a Slack outage never breaks callback idempotency) and **deduped on the stable `approval:<jobId>:<stage>` key** via the `slack_notifications` table (`INSERT … ON CONFLICT DO NOTHING`) — NOT the per-delivery approval id, so reconciler re-delivery under a fresh `event_id` does not re-ping (the same over-count trap the taste write-path avoids). When OFF (default) the callback path is byte-identical to today (no DB write, no Slack call). v1 deliberately covers approval-required only; completion/failure pings and inbound approve-from-Slack (reaction/reply → resume the pipeline) are deferred to later PRs (inbound mutates prod marketing state from an external webhook and gets its own focused review). Requires the `slack_notifications` table from `scripts/init-db.js` / `migrations/20260609010000_slack_notifications.sql` (applied on container start). Process-wide; default OFF in `docker-compose.yml`. **Per-tenant (Option A, T3):** `notifyApprovalRequired` now resolves the channel + bot token via `loadSlackConfigForTenant(tenantId)` (`backend/integrations/slack/config-store.ts`) instead of a shared global env channel — each tenant connects their OWN Slack workspace via OAuth and the notifier posts that tenant's token into that tenant's channel (closes the cross-tenant disclosure gap). When a tenant has no Slack connection the resolver returns null and the notification is skipped (`reason:'no_tenant_config'`), with **no** silent global fallback. `SLACK_SINGLE_TENANT_CHANNEL` + `SLACK_BOT_TOKEN` (BOTH required) is an explicit single-tenant / self-host opt-in that **replaces the removed `SLACK_NOTIFY_CHANNEL`** global path; it is never a default. `SLACK_CLIENT_ID`/`SLACK_CLIENT_SECRET` drive the per-tenant OAuth connect/token-exchange flow. The resolver is fail-open (any DB/decrypt error → null → skip, never throws) and the decrypted bot token is never logged. All Slack env vars (`ARIES_SLACK_NOTIFICATIONS_ENABLED`, `SLACK_BOT_TOKEN`, `SLACK_SINGLE_TENANT_CHANNEL`, `SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`) are wired into the `aries-app` service `environment:` block in `docker-compose.yml` (no `env_file`; the two-place rule) — set them in the host `.env`; only that service needs them since the reconciler runs inside it.

- `ARIES_NATIVE_REPLY_ENABLED=1` — rollout switch for native comment reply (qa-defect #598; `isNativeReplyEnabled`, `backend/integrations/meta-reply-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF the `POST /api/insights/comments/[commentId]/reply` endpoint is invisible — it returns a real 404 (not a stub) and touches no DB and no Graph API. When ON, an operator reply to a stored social comment is posted to Meta (Instagram `POST /{ig-comment-id}/replies`, Facebook `POST /{comment-id}/comments` — public comment reply, not Messenger/private_replies) via `replyToComment` (`backend/integrations/meta-reply.ts`), and on a confirmed Graph reply id the `insights_comments` row is marked (`is_replied=true`, `platform_reply_id`, `replied_at`). Tenant-isolated (the row is loaded `WHERE id=$1 AND tenant_id=$2`; a cross-tenant id 404s) and idempotent: the row is claimed (`is_replied` false→true) BEFORE the Graph call and rolled back only when the reply definitely never posted — the exact publish-path claim/rollback/outcome-unknown semantics (a 2xx with no reply id is `needs_manual_reconciliation`, never auto-retried). The Graph call runs OUTSIDE any held pool client (independent `pool.query` before/after). Responses are frontend-safe only (never the token or raw Graph error). Requires the `instagram_manage_comments` (IG) / `pages_manage_engagement` (FB) Graph scopes — requested in `backend/integrations/provider-registry.ts` but inert until Meta App Review grants them (external blocker) — plus the `platform_reply_id`/`replied_at` columns from `scripts/init-db.js` / `migrations/20260616000000_insights_comments_native_reply.sql` (applied on container start). Process-wide; default OFF.

- `ARIES_COMPOSIO_RECONCILER_ENABLED=1` — rollout switch for the Composio connection reconciler sidecar (`aries-composio-reconciler-worker` / `scripts/automations/composio-connection-reconciler-worker.ts` / `backend/integrations/composio/reconcile-pending-connections.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF, the container idles cleanly (no restart loop, no DB writes) and ships dormant — same dormant pattern as the draft-expiry and hermes-gc workers. When ON, every tick the worker queries `connected_accounts WHERE status='pending' AND updated_at > now() - (GRACE_MINUTES * interval '1 minute')` and calls `refreshConnectionStatus` for each row so a slow Composio INITIALIZING→ACTIVE transition (real cases took up to ~9 minutes) is reflected in the local DB without any operator action. Per-row failures are isolated (a single Composio API error does not abort the sweep for other tenants); the function is fail-safe and never throws. Only meaningful when `COMPOSIO_ENABLED=true`; when Composio is disabled `getAccountConnectionProvider()` returns null and the sweep is a no-op. Process-wide; default OFF in `docker-compose.yml` (`ARIES_COMPOSIO_RECONCILER_ENABLED:-0`). Requires `COMPOSIO_API_KEY` + `COMPOSIO_ENABLED=true` in the same environment block (the service wires all Composio auth-config vars).
- `ARIES_COMPOSIO_RECONCILER_INTERVAL_MS` — tick interval (ms) for the `aries-composio-reconciler-worker` self-scheduling loop. Default `60000` (60 seconds); a non-positive or unparseable value falls back. Only meaningful when `ARIES_COMPOSIO_RECONCILER_ENABLED` is on.
- `ARIES_COMPOSIO_RECONCILER_GRACE_MINUTES` — how many minutes back the reconciler sweeps. Only `connected_accounts` rows with `updated_at > now() - (GRACE_MINUTES * interval '1 minute')` are included. Default `30`; a non-positive or unparseable value falls back. Keeps the sweep scoped to recently-initiated connections and avoids re-checking rows that Composio's own ~10-minute INITIALIZING expiry has already resolved.
- `ARIES_IMAGE_EDIT_ENABLED=1` — rollout switch for IMAGE EDIT (image-to-image) of an existing creative (`isImageEditEnabled`, `backend/marketing/image-edit-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF, the `POST /api/social-content/jobs/[jobId]/creatives/[creativeId]/edit` endpoint is invisible — it returns a real 404 (not a stub), touches no DB and no Hermes gateway — and the review drawer's "Edit this image" section is hidden (the drawer is byte-identical to today). When ON, an operator's natural-language edit instruction is routed to Hermes as a new aries_run that reuses the regenerate submission path (per-stage profile pipeline scoped by `regenerate_creative`), but carries an `edit_instruction` plus the source image's Hermes cache basename so the content-generator profile calls `image_generate` on that existing image (its image-to-image *edit* endpoint, selected deterministically by `use_edit = bool(source_images)`) instead of generating from scratch — an explicit "IMAGE EDIT EXECUTION CONTRACT" in the submission prompt pins the agent to the edit tool (mirroring the production stage's "MUST call image_generate" contract). `editCreativeAsImageEdit` (`backend/marketing/regenerate-creative.ts`) resolves the source basename from `creative_assets.storage_key` for `runtime_asset` rows only (framed/uploaded `ingested_asset` rows live under DATA_ROOT, not the Hermes cache, so they resolve to null and Hermes falls back to locating the source via `source_run_id`+`source_creative_id`); resolution is fail-open (any DB/parse error → null → still submits). The edited image lands as a NEW `creative_assets` row (same `variant_batch_id`/`variant_index`, newer `created_at`) via the unchanged production-callback ingest — the original row is preserved, exactly like regenerate. Tenant-isolated and flag-gated taste signal on edit (`rejected` on the brand visual-style lens, best-effort, no-op unless `ARIES_POST_EDIT_TASTE_LEARNING_ENABLED`). The review-page server component reads the flag and passes `imageEditEnabled` down to the client drawer. No `aries-hermes-protocol` schema change (edit fields ride the existing free-form `input` string + the optional `regenerate_creative` context); no Hermes-repo change. Wired into the `aries-app` service `environment:` block in `docker-compose.yml`. Process-wide; default OFF. **Before flipping in prod, screenshot-verify a freshly edited + published post on a live tenant** (only rendered output counts). The deterministic-enough open question — whether the content-generator agent reliably honors the edit contract — needs a live smoke test; if it ever ignores the source image the feature degrades safely to a regenerate.

- `ARIES_TASK_TELEMETRY_ENABLED=1` — rollout switch for the AA-159 task-execution log (`isTaskTelemetryEnabled`, `backend/telemetry/task-telemetry-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When ON, each classified task execution appends one row to `task_execution_log` (`scripts/init-db.js` + `migrations/20260722000000_task_execution_log.sql`) tagged with its processing engine: **`AI_LLM`** (work run by a model behind the Hermes gateway — cost-bearing), **`DETERMINISTIC_RULE`** (rule-based automation: sidecar sweeps, schedulers, dispatchers), **`LOCAL_EDGE`** (in-process CPU work: sharp compositing, ffmpeg reel assembly). The two zero-cost engines log a **hard `0`** for `input_tokens`/`output_tokens`/`cost_cents` (zero by construction) plus measured `duration_ms` + `cpu_ms` from `process.cpuUsage()`, and never carry model columns; AI rows record `model_requested` (the hint Aries SENT, e.g. `HERMES_COMMENT_CLASSIFY_MODEL`) and leave `model_reported` / tokens / `cost_cents` **NULL** — Hermes owns model routing and does not report the resolved model or usage back to Aries, and NULL deliberately means "not reported", never "free" (no cost is ever synthesized from a price table; capturing real routing/usage needs a Hermes-side change). Writes are best-effort and never throw (`recordTaskExecution` / `withTaskExecutionLog`, `backend/telemetry/task-execution-log.ts`), and every wired call site passes its **already-held** db handle so telemetry adds no second pooled connection (guardrail #1). When OFF (default) nothing is written, no DB round-trip happens, and every wrapped task is a byte-identical pass-through. Wired call sites — **AI_LLM:** `handleHermesRunCallback` (`backend/execution/hermes-callbacks.ts`, the single post-dedup convergence point for EVERY Hermes run of either domain, so it covers all four marketing pipeline stages, the reconciler-delivered completions, and route workflows; logs **terminal deliveries only**, keyed `marketing.stage.<stage>` or `execution.<workflow_key>`, so a `running`/`requires_approval` progress ping never multiply-counts a stage and a reconciler re-delivery hits the existing duplicate-event guard) + the insights comment-classification leg. **DETERMINISTIC_RULE:** `runDraftExpirySweep`, `runGcMissingHermesAssets`, `sweepAbandonedSyncRuns`, `runFeedbackRetrySweep`. **LOCAL_EDGE:** `applyBrandFrameDetailed`, `composeReelMarketingLayer`, `synthesizeStillToVideo`. `ExecutionRunRecord` also carries an optional `execution_engine: 'AI_LLM'` stamp (absent on pre-AA-159 records — treat as unclassified, never defaulted). Deliberately NOT wired: the scheduled-dispatch publish route (each dispatch already has a `scheduled_post_dispatches` row with status + timing, so deterministic publish work is already queryable) and `runStaleRunReaper` (a pure-filesystem module — adding a DB dependency to it costs more than the row is worth). **AA-158 extends the same row** with per-user/per-task attribution and the token/timing payload: `user_id` (NULL on the majority of rows **by design** — the weekly cron, every sidecar, the reconciler and all Hermes callbacks are userless; only a route-initiated task has a session user), `task_id` (the per-EXECUTION id — `aries_run_id`/job id — as opposed to `task_key`, the task TYPE), `end_time`, the `prompt_tokens`/`completion_tokens`/`total_tokens` vocabulary (`total_tokens` is derived from the pair only when BOTH halves are known — a partial sum would silently understate spend), and `attempt_number` + a third status value **`retry`** (a non-terminal attempt that will be retried, logged as its own event so retry spend is separable: summing `succeeded`+`failed` gives settled cost, summing all rows gives true spend). Emission is **non-blocking**: `emitTaskExecution` buffers in memory and returns synchronously, and a macrotask-scheduled flush writes the whole buffer as ONE multi-row INSERT (`flushTaskExecutionBuffer`); the buffer is bounded at 500 events and drops the OLDEST (counted, logged) rather than growing unbounded, so a stalled database degrades to missing telemetry and never to backpressure on publishing. `recordTaskExecution` remains the awaited path for callers that hold a client they are about to release. Token capture is plumbed end-to-end but **inert**: protocol `1.3.0` adds an optional `usage` block (`prompt_tokens`/`completion_tokens`/`total_tokens`/`model`) to the run callback, which `handleHermesRunCallback` persists — Hermes does not emit it yet, so those columns stay NULL until a Hermes-side change lands, at which point they fill in with no further Aries work. Process-wide; default OFF in `docker-compose.yml` on `aries-app`, `aries-draft-expiry-sweep-worker`, and `aries-insights-sync-worker`. Note the pre-existing `insights_llm_calls` table is still dead/unwired and is a separate delete-or-migrate decision.

- `ARIES_USAGE_ROLLUP_ENABLED=1` / `ARIES_USAGE_RETENTION_ENABLED=1` — the AA-161 usage time-series layer over the SAME raw table (`usageRollupEnabled` / `usageRetentionEnabled`, `backend/telemetry/usage-rollup-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled; both default OFF. There is deliberately **no second raw usage table**: `task_execution_log` IS the raw event log, and the read-only view `usage_events` aliases it into the ticket's vocabulary (`company_id` → `tenant_id`, `created_at` → `started_at`). The layer is **rollup TABLES, not `MATERIALIZED VIEW`s** — an MV can only refresh whole (a full rescan of a high-volume log each time, and `REFRESH CONCURRENTLY` needs a unique index) and, decisively, an MV is destroyed by the 90-day raw purge while daily aggregates must be kept indefinitely; real tables outlive their source rows. `runUsageRollup` (`backend/telemetry/usage-rollups.ts`) rolls `[watermark − re-roll overlap, last CLOSED hour)` into `usage_rollup_hourly` as a full per-bucket recompute UPSERTed with `= EXCLUDED` (idempotent — re-rolling converges, never accumulates), then rebuilds the WHOLE days/months those hours touch from the hourly table (so the derived grains stay correct after the raw rows are gone), then advances the monotonic watermark in `usage_rollup_state`. Buckets are **UTC** (`date_trunc(... AT TIME ZONE 'UTC') AT TIME ZONE 'UTC'`) so a differing session TimeZone cannot shift a day boundary; tenant-local reporting is a read-time concern. NULL `tenant_id`/`user_id` collapse to the **0 sentinel** in the rollup keys — a NULL inside the PK would break UPSERT idempotency (NULL <> NULL), and both id sequences start at 1. Token columns stay NULL when unreported ("not reported", never "free" — Hermes still does not emit usage), which is why every grain also carries `ai_events` vs `ai_events_with_usage`: without that denominator a $0 bucket reads as "no spend" when the truth is "nothing reported its spend". **Retention** (`runUsageRetentionSweep`, `backend/telemetry/usage-retention.ts`) keeps raw for `ARIES_USAGE_RAW_RETENTION_DAYS` (default 90) and hourly for `ARIES_USAGE_HOURLY_RETENTION_DAYS` (default 400); **daily and monthly are never swept**. Its load-bearing guard is the **watermark interlock**: the cutoff is `min(now − retention, rolled_through − re-roll overlap)`, and with **no watermark at all it deletes nothing** — so a raw row can never be purged before it has been aggregated, and a re-roll can never overwrite a correct aggregate with a zeroed one. Deletes are batched (`ARIES_USAGE_RETENTION_BATCH_SIZE` semantics, oldest-first) so a first purge never takes one long lock on the log live writers are inserting into. Indexing for the AC filter set adds `(tenant_id, execution_engine, started_at DESC)` (the billing query) and `(started_at)` (the cross-tenant rollup/purge scan) on the raw table, plus tenant-leading indexes on each rollup and a partial per-user index on daily. **Before first enabling retention in prod, run one cycle with `ARIES_USAGE_RETENTION_DRY_RUN=1`** (counts only, zero deletes), then flip it back to commit. Neither module is wrapped in `withTaskExecutionLog` — a metering pass that meters itself would append a row to the very table it aggregates (and the purge, to the one it purges) on every tick forever. Note both are useless until `ARIES_TASK_TELEMETRY_ENABLED` is ON: with telemetry off the raw log stays empty and every rollup is a no-op. Process-wide; both default OFF in `docker-compose.yml` on the `aries-usage-rollup-worker` service. **AA-162** adds the coarse consumer surface on top: the materialized view **`daily_company_usage`** (one row per `company_id` × UTC `usage_date`, projecting `total_tokens` / `total_duration_ms` / `total_cogs_cents` / `total_tasks` for dashboards and chargeback), refreshed `CONCURRENTLY` at the end of each rollup pass — no extra flag, it rides `ARIES_USAGE_ROLLUP_ENABLED`. An MV is the right tool *here* and not over the raw log for one specific reason: its source is `usage_rollup_daily`, which is already aggregated, small, and never swept by retention, so both AA-161 objections (full rescan per refresh; destroyed by the 90-day purge) vanish. `CONCURRENTLY` keeps dashboard reads unblocked and **requires** `idx_daily_company_usage_company_date` — that unique index is load-bearing, not a performance nicety — and cannot run inside a transaction block. The refresh is best-effort and skipped when the pass changed no daily bucket: a failure degrades to a stale view, never a failed pass or a rewound watermark. **`total_cogs_cents` reads 0/NULL for every company today and that is truthful** — `cost_cents` is a hard 0 on the zero-cost engines and NULL on every AI row until Hermes reports usage, and no cost is ever synthesized from a price table — which is why `ai_tasks` and `tasks_with_usage_reported` ride alongside the sums; a chargeback that reads the COGS column without those denominators will silently bill zero. `company_id = 0` is the "not scoped" sentinel (system sweeps, cron, callbacks), kept so the view reconciles against the raw log; a customer-facing surface must exclude it explicitly. Full-refresh cost scales with `usage_rollup_daily`, which is trivial at the current profile; if tenant count grows by orders of magnitude the escape hatch is converting it to an incrementally-maintained table on the same watermark.

- `ARIES_PLAN_ENFORCEMENT_ENABLED=1` / `ARIES_PLAN_ENFORCEMENT_METRIC=tasks|tokens` — the AA-163 tiered plan gate (`isPlanEnforcementEnabled` / `resolvePlanEnforcementMetric`, `backend/billing/plan-enforcement-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF; metric defaults to `tasks`. **Four tiers** — Starter (Small) / Growth (Medium) / Scale (Large) / Enterprise (Custom) — live as **rows** in `plan_rate_cards` (`scripts/init-db.js` + `migrations/20260729000000_plan_rate_cards.sql`), not code constants, because the AC is "as a PM I want to *configure* thresholds and rates": the seed is `ON CONFLICT DO NOTHING` so an edited rate **survives every redeploy**, and `backend/billing/rate-cards.ts` holds the shipped defaults as the seed + last-resort fallback. A **NULL allowance means UNLIMITED, never a 0 ceiling** — that is how Custom/Enterprise works (that tier plus the `monthly_*_allowance_override` columns on `company_subscriptions`, one row per company, FK+cascade to `organizations`), so "Custom" needs no bespoke code path. The tier lives on the **company**, deliberately NOT on `users.plan` — that column is the per-*account* multi-workspace entitlement (Decision 13) on a different axis, and folding tiers into it would break `assertMultiWorkspaceEntitlement`. Writer is the CLI `scripts/billing/set-company-plan.ts` (mirrors `set-user-plan.ts`; a payments PR later replaces only the writer, touching zero enforcement code); its `--tasks none` clears a negotiated ceiling. **The rate card is DECLARATIVE**: `cost_per_million_tokens_cents` records configured pricing and *nothing multiplies by it* — `daily_company_usage.total_cogs_cents` stays `sum(cost_cents)` from the raw log, so no bill is ever synthesized from a price table (pinned by a test that fails if the gate ever reads the price column). **Enforcement runs on TASK COUNTS**, not tokens: every `AI_LLM` row has NULL tokens because Hermes does not report usage back, so a token gate would compare a ceiling against a permanently-zero counter; both allowances ship so the metric flips with an env var and no migration, and `metric=tokens` set early is safe-but-inert (an unmetered metric always allows). `assertUsageWithinPlan` (`backend/billing/usage-entitlement.ts`) resolves the subscription + tier card, reads period-to-date consumption for the **current UTC calendar month** from `daily_company_usage`, and is **fail-open on every uncertainty** — flag off, non-company/system work, unlimited allowance, no rollup watermark (metering off ⇒ "0 used" is an artifact, not a measurement), unreported metric, or a DB error on either lookup all ALLOW with the reason recorded; it denies only when a company is positively over a **known** ceiling. A company with no subscription row is covered by the entry tier (so a workspace created after the backfill is gated without waiting for a container restart). The single call site is `enforcePlanLimitOrThrow(input.tenantId)` at the top of `startSocialContentJob` — the convergence point for every content-generation entry (dashboard forms, weekly trigger, onboarding, reel companion) — placed **before the job id is minted** so a denied create leaves no partial state; it throws `plan_limit_exceeded:<tier>:<metric>`, which `lib/marketing-create-errors.ts` maps to **402 `plan_limit_exceeded`** with the inner detail collapsed out (same shape as the multi-workspace paywall). Background/system AI work (sidecar sweeps, comment classification, posting-time research) is deliberately NOT gated: quota-denying a background sweep creates silent data gaps in features already paid for. Consumption lags real usage by up to one rollup interval (default 1h), so a company can overshoot by that much — exact metering is unattainable while tokens are unreported, and a raw-log scan on the job-create path would cost real latency for precision that cannot be used (guardrail #1). Requires `ARIES_USAGE_ROLLUP_ENABLED` to enforce anything at all. Process-wide; wired into the `aries-app` service `environment:` block in `docker-compose.yml`; default OFF.

- `ARIES_QUOTA_ALERTS_ENABLED=1` / `BILLING_CHECKOUT_PROVIDER` — the AA-164 customer-facing capacity surface: balance display, purchased credits, and 80/95% exhaustion alerts. **Credits are TASK credits**, the same unit AA-163 enforces on — a token balance would be unspendable and a token percentage would never move, since every AI row has NULL tokens until Hermes reports usage; when that lands the metric flips via `ARIES_PLAN_ENFORCEMENT_METRIC` and the ledger's unit follows. `company_credit_ledger` (`scripts/init-db.js` + `migrations/20260730000000_*.sql`) is **append-only** — a balance is `sum(credits)` over unexpired rows, never a mutable counter (which loses its audit trail the first time two writers race), and a mistake is reversed by appending a negative `correction` row. Credits **stack on top of** the monthly allowance and do **not** reset with the calendar month (voiding paid capacity at a month boundary would be taking money for nothing). The partial UNIQUE on `external_event_id` is what makes future gateway fulfillment idempotent under webhook redelivery — double-crediting one payment is a money bug. **v1 writer is the CLI `scripts/billing/grant-company-credits.ts`** (`grantCompanyCredits`, `backend/billing/credit-ledger.ts`), the same seam as `set-user-plan.ts`/`set-company-plan.ts`: **there is no payment gateway connected yet**, so the checkout PR adds a webhook that calls that one function with `source:'purchase'` + the gateway event id and touches no consumption or enforcement code. `BILLING_CHECKOUT_PROVIDER` (unset by default) is the single server-side predicate for "a gateway exists"; until it is set, `GET /api/billing/quota` reports `purchaseEnabled:false` and the settings card renders a contact-us line rather than a button that cannot complete. `loadQuotaSummary` (`backend/billing/quota-summary.ts`) is the ONE place quota math lives — shared with the gate through `resolveIncludedAllowance`, so the number a customer is **shown** and the number they are **cut off at** cannot drift. **Honesty contract:** with no rollup watermark it reports `metered:false` with NULL usage and the card says usage isn't being tracked, instead of drawing a confident `0% used`; `percentUsed` is deliberately **not clamped** at 100 (an overage during the rollup lag really is 104%); an unlimited plan has no percentage at all. The quota route is readable by **any** authenticated role (hiding remaining capacity makes "why did generation stop" unanswerable for most of the team) while `canPurchase` is `tenant_admin`-only per the AC, the company id comes **only** from tenant context, and a summary failure is a `503`, never a zeroed body. **Alerts** (`runQuotaThresholdAlerts`, `backend/billing/quota-alerts.ts`) ride the `aries-usage-rollup-worker` tick rather than a ninth sidecar — one query gathers every company's allowance/consumption/credits (no fan-out, guardrail #1), and the `(company_id, period_start, threshold)` PK on `usage_alert_notifications` **is** the dedupe. The row is **claimed BEFORE the send**: a crash mid-send costs one alert, whereas claiming afterwards would re-email hourly until an attempt succeeded. A company with **no admins is left unclaimed** so the alert can still land once someone is made an admin; unmetered/unlimited/unreported companies are skipped entirely; per-company failures are isolated. The trigger keys off the **same rounded percentage the dashboard shows**, so a customer can never receive an "80%" email while their dashboard reads 79%. Alerts inherit the rollup lag (up to one tick), so a 95% alert can arrive past 100% — the email therefore states the real percentage, not the threshold. Requires `ARIES_USAGE_ROLLUP_ENABLED` plus `RESEND_API_KEY`/`EMAIL_FROM`/`APP_BASE_URL`; wired into the `aries-usage-rollup-worker` service `environment:` block. Process-wide; default OFF.

- `ARIES_WEEKLY_CROSSPOST_ENABLED=1` — rollout switch for the weekly cross-post fan-out (producer side; `isWeeklyCrosspostEnabled`, `backend/marketing/weekly-crosspost.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF, `synthesizePublishPostsFromContentPackage` synthesis output is **byte-identical** to today (FB/IG rows only — a golden invariant, `tests/synthesize-publish-posts-surface.test.ts`). When ON, for every FB/IG **feed image** post it synthesizes (surface `feed`, media_type `image` ONLY — never a story/reel), it additionally synthesizes one extra `posts` row per platform in `CROSSPOST_PLATFORMS = ['x','linkedin','reddit']` where (a) that platform's rollout flag (`ARIES_X_ENABLED`/`ARIES_LINKEDIN_ENABLED`/`ARIES_REDDIT_ENABLED`) is ON **and** (b) the tenant has an active (`status='connected'`) `connected_accounts` row for it — resolved ONCE per synthesis call via a single `connected_accounts` query (`resolveCrosspostPlatforms`, no `Promise.all` fan-out), **fail-open to `[]` on any DB error** so a crosspost lookup failure never breaks FB/IG synthesis. Each extra row carries the SAME rendered feed image (`creative_asset_ids`) as the FB/IG row and a **platform-adapted caption** (`adaptCaptionForPlatform`): X = the caption's first sentence/hook + up to 2 hashtags, hard-capped at 270 weighted chars (URLs count 23, CJK/emoji count 2, word-boundary ellipsis, never empty); LinkedIn = full caption clamped to 2900; Reddit = `title\n\nbody` where the title is the first sentence (hashtags stripped, clamped 280) — the reddit publisher's `redditTitleFromContent` reads line 1 as the title and posts the body. The idempotency key stays `${jobId}:${postNumber}:${platform}:feed`, so a reconciler re-delivery hits `ON CONFLICT DO NOTHING` and never duplicates. The whole fan-out is wrapped so any error degrades to no-crosspost with a `console.warn`; the `VALID_PLATFORMS` Hermes-input filter is unchanged (fan-out is deterministic Aries-side). The publish back-half for these platforms is already code-complete (`composio-publisher-provider.ts` + the scheduled-dispatch admit gate + `ALLOWED_TARGET_PLATFORMS`); the publish queue renders non-IG/FB rows as a labelled status row with no manual publish button (auto-schedule handles delivery in v1). Wired into the `aries-app` service `environment:` block in `docker-compose.yml`. Process-wide; default OFF. Go-live also needs the per-platform `ARIES_<P>_ENABLED` + `COMPOSIO_<P>_PUBLISH_POST_ACTION` slugs; Reddit needs `COMPOSIO_REDDIT_TARGET_SUBREDDIT` or falls back to the connected user's own profile.

- `ARIES_AI_POSTING_TIMES_ENABLED=1` — rollout switch for AI-derived per-platform posting times (`isAiPostingTimesEnabled`, `backend/marketing/posting-times-env.ts`; advisor in `backend/marketing/posting-time-advisor.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF, post scheduling is **byte-identical** to today: the auto-schedule paths use the hardcoded `PLATFORM_POSTING_DEFAULTS`, no derivation runs, and no `marketing_posting_times` rows are read or written. When ON, **every content-generation start** (one fire-and-forget hook at the `startSocialContentJob` convergence point, so the dashboard create forms, the "Generate this week's content" button, the weekly trigger, onboarding, and the reel companion are all covered) re-derives WHEN posts publish, per platform (FB+IG always, plus flag-ON+connected crosspost platforms): (1) **analytics source** — once the tenant has ≥ `ARIES_POSTING_TIMES_MIN_ANALYTICS_POSTS` (default 8) own posts with insights metrics on that platform, best hour + ranked days come from the tenant's own engagement (latest-snapshot per post — never SUM across `insights_post_metrics_daily` dates, which over-counts — bucketed by tenant-local DOW×hour of publish); (2) **competitor source (cold start)** — otherwise ONE raw Hermes research run (same submit+poll idiom as `classify-comments.ts`; `HERMES_POSTING_TIMES_MODEL` overrides the model hint) analyzes the business profile's `competitor_url` and recommends per-platform days/hours; a job `competitor_url` that merely defaulted to the tenant's own brand URL is treated as "no competitor set". Results upsert into `marketing_posting_times` (one row per tenant×platform, `scripts/init-db.js` + `migrations/20260709120000_*.sql`); `autoScheduleApprovedPostsForJob` reads them **fail-open** (`loadPostingTimeOverrides` → null on flag-off/missing/error → defaults) and applies them to the FEED slot only (story/reel keep platform windows; stagger preserved; ranked days re-anchor a pair on the default-cadence path only when honoring them cannot drop post volume). Dedupe is three-layered and ordered read-first: a TTL freshness check (`ARIES_POSTING_TIMES_TTL_MINUTES`, default 60; 0 = every click; force bypasses the TTL but honors a 2-min cooldown floor — an admin looping the button can't fire back-to-back research runs; TTL=0 disables the floor too), an in-process in-flight Set (fast path), and a **cross-process atomic claim row** (`marketing_posting_time_claims`, conditional `ON CONFLICT ... WHERE claimed_at` stale-window upsert mirroring the `marketing_schedule` claim idiom, 10-min window, honored even under force — so concurrent derivations across the `ARIES_WEB_CONCURRENCY` cluster workers collapse to one; the claim is retained as failure backoff when the competitor research leg fails TRANSIENTLY — independent of other platforms succeeding via analytics — so a Hermes outage can't re-fire a doomed research run on every click; permanent no-ops (no competitor configured) release it; TTL-skips never touch the claims table; the claim window auto-scales with HERMES_POSTING_TIMES_TIMEOUT_MS). The competitor run's Hermes session is per-tenant (`aries-posting-times-<tenantId>`; `HERMES_POSTING_TIMES_SESSION_KEY` overrides only the PREFIX — tenant isolation cannot be configured away) and competitor-derived rationales are URL-stripped before persisting; the settings-hub "Posting times" card (`GET /api/marketing/posting-times`, admin `POST .../derive` → 202 fire-and-forget, real 404 when the flag is off) shows the derived rows with source badges and an **Update times now** button that polls until the rows refresh. Derivation is best-effort and never blocks or fails the generation request. Wired into the `aries-app` service `environment:` block in `docker-compose.yml`. Process-wide; default OFF.

- `ARIES_MULTI_WORKSPACE_ENABLED=1` — rollout switch for multi-workspace membership-aware auth resolution (Phase 1 of `docs/plans/2026-07-03-multi-workspace-membership.md`; `isMultiWorkspaceEnabled`, `backend/tenant/multi-workspace-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF, tenant claims/context resolution is **byte-identical** to the single-pointer model (`users.organization_id` + `users.role`) — pinned by `tests/auth/tenant-resolution-flag-off-golden.test.ts`. When ON: (1) the consolidated claims helper (`resolveTenantClaimsRow`, `lib/auth-tenant-membership.ts` — the ONE place the claims join lives) resolves via a single indexed `users ⋈ organization_memberships ⋈ organizations` query — the pointer is honored only when an **ACTIVE** membership backs it, the role comes from the **membership row** (never `users.role`, which becomes a legacy mirror), and `workspaceCount` (active-membership count) rides the same statement (no second aggregate; `auth()` runs several times per render); (2) a pointer to an existing org with NO membership row **self-heals** one `active` membership derived from the pointer + `users.role` (`ON CONFLICT DO NOTHING` — never flips an `invited` row); (3) `ensureTenantAccessForUser` stops minting personal orgs: a NULL/invalid pointer with N≥1 active memberships repoints to the deterministic default (most-recent `last_active_at`, else oldest) updating pointer + role mirror atomically, and a **zero-membership** account mints NOTHING — sign-in proceeds claims-less and lands on the invite-aware workspace chooser (`/workspace/choose`, outside the gated dashboard layout); API routes surface `403 tenant_membership_missing` for that state and the jwt hydrate **clears** stale tenant claims (no ghost claims feeding the DB-outage fallback). System actors (`userId:'system'`, INTERNAL_API_SECRET routes, workers) never hit membership validation. Wired into the `aries-app` service `environment:` block in `docker-compose.yml`. Process-wide; default OFF — leave OFF until the program's rendered-UI verification (invite → accept → switch on live prod).

- `ARIES_WEEKLY_REEL_ENABLED=1` — rollout switch for the weekly-reel companion job (`maybeFireWeeklyReelJob`, `backend/marketing/weekly-reel-trigger.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled (`isWeeklyReelEnabled`, `backend/marketing/weekly-reel-env.ts`). Default OFF. When ON, each `weekly_social_content` job fires a separate `one_off_post` reel job (9:16 vertical, `videoRenderCount=1`, `imageCreativeCount=0`) so video production never contends with the weekly image pipeline's single production-stage slot. The companion is non-blocking (fired `void …catch(()=>{})`) so its Hermes submission latency never delays the weekly endpoint. Idempotent: a deterministic `createdBy = "reel:<sourceWeeklyJobId>"` marker is stamped on the reel job, and `findRecentJobIdForTenant` scans the last 7 days before firing so a reconciler re-delivery or a re-fire collapses onto the existing job. **Also requires `ARIES_VIDEO_PUBLISH_ENABLED=1`** — when video publishing is off the companion returns early (`reason:'video_publish_off'`) with no Hermes submission. **Reel-companion scope clamp (2026-07-13 incident):** the Hermes agent sometimes emits a full 7-post weekly package on a reel job; `synthesizePublishPostsFromContentPackage` therefore only synthesizes reel/video shapes (and no story promotion) for a `created_by` starting `reel:` — the parent weekly job is the sole owner of the week's feed posts, and the weekly trigger now sets `publishRequested:true` so that parent takes the real publish path (previously its posts stranded approved-unscheduled while the reel job's rogue feed posts published instead). **Reel no-video outcome contract (2026-07-06/13 incidents):** the Hermes content-generator only calls video_generate ~50% of the time on reel jobs; when it doesn't, (a) `synthesizePublishPostsFromContentPackage` DROPS any video-shaped (post, platform) target that has no ingested `media_type='video'` creative_asset (never a dead reel post; when a video asset exists the reel links THAT asset + its dims, not the post-number-mapped image), (b) the dispatch route fails a video post with no video media TERMINALLY (`no_video_asset`, retryable:false — no retry-spam, no manual defusing), and (c) the reel-companion outcome gate (`backend/marketing/reel-video-outcome.ts`, called at every terminal-completion site in `hermes-callbacks.ts`) flips a COMPLETED `created_by reel:*` job with no video `posts` row to **failed** (`reel_video_asset_missing`, production-stage failure + runtime incident) and fires ONE automatic retry job (`maybeFireReelVideoRetryJob`, marker `reel:retry:<failedReelJobId>` — starts with `reel:` so the clamp+gate still apply; a failed retry never retries itself; idempotent across reconciler re-delivery; fail-open on DB lookup errors so a blip never fails a healthy job). Strengthening the Hermes-side MUST-call-video_generate contract is a separate Hermes-repo change. Process-wide; default OFF in `docker-compose.yml`.
- `ARIES_REEL_VOICEOVER_ENABLED=1` — deployment-level **capability gate** for ElevenLabs TTS voiceover on reels (`isReelVoiceoverEnabled`, `backend/integrations/elevenlabs/voiceover-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. The *code* default when the env var is entirely unset (e.g. some tests) is OFF, but the **shipped `docker-compose.yml` default is ON (`:-1`)** — see the end of this entry; set `0` to disable. This flag no longer *forces* voiceover — it only **permits** it. What audio a given reel carries is the per-tenant / per-job **reel audio mode** (`music` | `voiceover` | `both`; canonical in `backend/marketing/reel-audio-mode.ts`): the per-job override (create-form "Reel audio" select → `doc.inputs.request.reelAudioMode`) wins over the per-tenant Settings default (`business_profiles.reel_audio_mode`, read at ingest via `loadTenantReelAudioModeOrNull`), which wins over the global default (`DEFAULT_REEL_AUDIO_MODE = 'music'`). `composeReelMarketingLayer` synthesises a VO script from the reel's hook/value/CTA copy via `fitCopyToDuration` + `synthesizeVoiceover` and mixes the mp3 under the brand music bed (ducked to −15 dB) using `amix=duration=longest` + `apad` (audio padded to the video length; `-shortest` bounds output to the video, never the shorter VO) — but **only** when the resolved mode wants voiceover **AND** this flag is ON **AND** `ELEVENLABS_API_KEY` is set. The pure decision (`resolveReelAudioComposition`) guarantees: `music` → music bed only; `voiceover` → VO only, with a music **fallback** (never a silent reel) when the capability is off / key absent / synthesis fails; `both` → VO ducked over music (→ music only when VO unavailable). When this flag is OFF or the key is absent, every reel is music-bed-only regardless of mode — byte-identical to before this feature. Best-effort: any TTS failure falls back silently to the music bed; the reel is never blocked. Process-wide; **default ON in `docker-compose.yml` (`:-1`)** — safe because the global default mode is `'music'`, so flipping the capability on changes no reel's audio until a tenant opts into voiceover.
- `ELEVENLABS_API_KEY` — API key for ElevenLabs TTS. Required when `ARIES_REEL_VOICEOVER_ENABLED` is on; absent = voiceover skipped silently (music-bed-only fallback). Set in the host `.env` and wired into the `aries-app` service `environment:` block (the two-place rule).
- `ELEVENLABS_VOICE_ID` — ElevenLabs voice id to use for reel voiceover. Defaults to `EXAVITQu4vr4xnSDxMaL` (ElevenLabs "Sarah" stock voice, available on all free/paid plans). Override with any voice id from your ElevenLabs account. Only meaningful when `ARIES_REEL_VOICEOVER_ENABLED` is on.
- `ARIES_WEEKLY_RESULTS_ENABLED=1` — rollout switch for the **weekly results report** panel on `/dashboard/results` (S5-1/AA-110, gap F1b; `isWeeklyResultsEnabled`, `backend/marketing/weekly-results-env.ts`). Aries treats `1`/`true`/`yes`/`on` as enabled. Default OFF. When OFF, `/dashboard/results` renders the legacy live-posts roster unchanged (the page reads the flag **server-side**, so a disabled deployment mounts no extra component and issues no fetch — no flash of a panel that then vanishes) and `GET /api/dashboard/weekly-results` returns `{enabled:false}` **before** resolving tenant context or taking a pooled client, so it costs nothing. When ON, the panel recaps the most-recent **completed** ISO week (Mon–Sun **UTC**, `?week=YYYY-WW` override): published counts by channel/surface, skipped (scheduled posts still `pending` after their slot passed), blocked (`dispatch_status IN ('failed','dead_letter')`), the top channel, best/weakest post, and one recommended next action. **The MVP slice is READ-ONLY** — no schema change, no write of any kind, so the rollback is the flag alone. **Honesty contract:** best/weakest render an explicit "engagement ranking isn't available yet" panel when this tenant has no `insights_accounts` row or no synced metrics in the window — **never a guessed winner** — and `topChannel` always states its `basis` (`reach` vs `published_count`), which follows per-tenant data availability. Ranking reads the shipped insights model through `LATEST_POST_METRICS_LATERAL` (S2-1: rows are lifetime-**cumulative**, so SUMming a post's dated rows inflates it ~N× and would crown whichever post has simply been synced longest); best and weakest come from **one** ordered query, inverted, so they can never disagree. It deliberately does **not** apply the S4-1 attribution scope (that flag ships default OFF; letting it change which posts this panel counts would make the numbers depend on an unrelated switch) and deliberately does **not** touch the `#513` / `ARIES_INSIGHTS_513_TABLES_PRESENT` path. The auth/"Reconnect Meta" signal comes from `oauth_connections.status='reauthorization_required'` (the #519 surface), because a persisted per-post `failure_class` describes dispatch taxonomy, not the connection's current health. `dispatch_status='manual_reconciliation'` is reported as its **own** count and never folded into `blocked`: the publish path parks those precisely because the outcome is unknown, so calling them blocked would assert something untrue. Queries run **sequentially** on one client (guardrail #1 — `/dashboard/results` is already on the slow list-hydration path). Wired into the `aries-app` service `environment:` block in `docker-compose.yml`. Process-wide; default OFF. Honcho perf-finding surfacing and the Approve/Edit/Reject promotion route are the **deferred** D.1/E slice (`docs/plans/2026-06-01-weekly-results-next-action.md`).

Required when working on Aries-managed OAuth providers: `OAUTH_TOKEN_ENCRYPTION_KEY`. Weekly social content media generation does not use an Aries-side OpenAI client or secret; Hermes owns media auth and execution.

Hermes image serving (set both together; see `docker-compose.yml` volumes):
- `HERMES_IMAGE_CACHE_DIR` — host-side path to the Hermes image cache (default `/home/node/.hermes/profiles/aries-content-generator/cache/images` — the profile-scoped cache the three-profile content generator writes to; the legacy `/home/node/.hermes/cache/images` is no longer written by the marketing pipeline); bind-mounted read-only into the container as `HERMES_IMAGE_CACHE_MOUNT`.
- `HERMES_IMAGE_CACHE_MOUNT` — in-container mount point for the Hermes image cache (default `/hermes-media`); read by `app/api/internal/hermes/media/[...path]/route.ts` to stream generated images to authenticated browser sessions, and by `backend/marketing/ingest-production-assets.ts` which resolves every Hermes-reported **image** `path` to `<mount>/<basename>` before reading. If the mount points at the wrong host directory, production-callback creative_assets ingestion silently inserts zero rows and the operator dashboard shows "No launch items".

Hermes **video** serving (separate mount — Hermes localizes generated video into a *different* dir than images; only meaningful with `ARIES_VIDEO_PUBLISH_ENABLED`):
- `HERMES_VIDEO_CACHE_DIR` — host-side path to the Hermes **video** cache (default `/home/node/.hermes/profiles/aries-content-generator/cache/videos` — the `video_gen` toolset writes mp4s here, a sibling of `cache/images`, NOT into the image cache); bind-mounted read-only into the container as `HERMES_VIDEO_CACHE_MOUNT`.
- `HERMES_VIDEO_CACHE_MOUNT` — in-container mount point for the Hermes video cache (default `/hermes-video-media`); `ingest-production-assets.ts` resolves a Hermes-reported **video** `path` (entries with `type:'generated_video'` / `media_type:'video'`) to `<video-mount>/<basename>`, reads the bytes, then persists a **durable copy under `DATA_ROOT` as an `ingested_asset`** (so the clip survives Hermes cache eviction and the `gc-missing-hermes-assets` sweep — runtime_asset-only — never orphans it). The `/api/internal/hermes/media/<id>` route serves it from `DATA_ROOT` like a framed image. If unset (or pointing at the wrong dir), a generated video is unresolvable and ingest skips it (image posts unaffected). This mount is the fix for "Hermes writes video to `cache/videos` but Aries only mounted `cache/images`" — without it, generated reels/stories never reach the DB.

Local dev defaults:
```bash
export DB_HOST=localhost DB_PORT=5432 DB_USER=aries_user DB_PASSWORD=aries_pass DB_NAME=aries_dev
export CODE_ROOT=/home/node/aries-app DATA_ROOT=/tmp/aries-data NODE_ENV=development
export APP_BASE_URL=http://localhost:3000 NEXTAUTH_URL=http://localhost:3000 AUTH_URL=http://localhost:3000 AUTH_TRUST_HOST=true
```

## gstack (REQUIRED)

gstack is required for all AI-assisted work in this repo. Verify with `test -d ~/.claude/skills/gstack/bin`. Install/usage instructions live in the global `~/.claude/CLAUDE.md`. Use `/browse` for all web browsing.

## Skill routing

When the user's request matches an available skill, invoke it via the Skill tool. When in doubt, invoke the skill.

Key routing rules:
- Product ideas/brainstorming → invoke /office-hours
- Strategy/scope → invoke /plan-ceo-review
- Architecture → invoke /plan-eng-review
- Design system/plan review → invoke /design-consultation or /plan-design-review
- Full review pipeline → invoke /autoplan
- Bugs/errors → invoke /investigate
- QA/testing site behavior → invoke /qa or /qa-only
- Code review/diff check → invoke /review
- Visual polish → invoke /design-review
- Ship/deploy/PR → invoke /ship or /land-and-deploy
- Save progress → invoke /context-save
- Resume context → invoke /context-restore
