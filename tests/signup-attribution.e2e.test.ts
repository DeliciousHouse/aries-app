import { test } from 'node:test';

test.todo(
  'signup attribution E2E: GET /signup?ref= → credentials or Google signup → outbox row → worker POST to VMS (run manually against docker-stack; see plan smoke test section)',
);
